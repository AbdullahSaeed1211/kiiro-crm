# L-20260914-M2-W5-68a728 repro

`edge-case.ts bad` models a lost-stage board drop that immediately applies the terminal stage and shows a failure after the required lost details are deferred. The invariant check exits nonzero.
