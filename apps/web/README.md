# web

## Purpose
Composition root: Next.js + Payload + OpenNext Worker for one tenant deployment.

## Public API
HTTP routes under `/api/v1`, the product UI and Payload admin at `/admin` (spec §12, §17).
