/** Field-config-driven record form composite (spec §15.4, §17.5). */
export { RecordForm, type RecordFormLabels, type RecordFormProps } from './RecordForm'
export {
  formValues,
  validateRequired,
  type FormValue,
  type RecordFieldConfig,
  type RecordFormValues,
  type SelectOption,
} from './form'
