# UX Reference Contract

This contract is the acceptance boundary for the shared tenant workspace surfaces. UI work uses six product and workflow references: Frappe CRM, Twenty, Plane, Huly, Corteza, and Odoo. Payload supplies the foundation and admin patterns; Agentic Inbox supplies the email-specific reference.

| Surface             | Owner               | Adopted contract                                                                                                                                      | Explicit deviation                                                                                  |
| ------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| Application shell   | Twenty              | In-flow sidebar, header utilities, inset main surface, contained overflow, consistent active rows                                                     | Tenant settings own branding, terminology, and module filtering                                     |
| Settings navigation | Twenty + Frappe CRM | Grouped, role-filtered navigation with the same row grammar as the primary shell; mobile remains a normal routed list                                 | No full-screen settings modal                                                                       |
| Command palette     | Plane               | Centered bounded dialog, useful default navigation/create groups, keyboard-selectable records, reset on close                                         | Remote search remains the product's per-record authorized API                                       |
| Task detail         | Twenty + Plane      | Task links open an intercepted contextual sheet while direct `/tasks/:id` remains the canonical full page; browser history represents the active task | Scroll containment and non-Calendar source routes still need browser evidence                       |
| Activity            | Plane + Huly        | Chronological event stream with comments read through the same parent scope                                                                           | Email is a dedicated record tab; reply delivery intentionally hands off to the tenant's mail client |
| Organizations       | Twenty + Frappe CRM | Compact, continuous directory grid with grouped controls; record opens on linked projects, contacts, and deals, with details in a secondary rail      | No organization image field, configurable field layout, or persistent directory view preset yet     |

## Source selection

The read-only source snapshots are pinned in `crm/references/README.md`. For a broad UI pass, inspect all six references and record which are relevant to each in-scope interaction. Choose the strongest pattern for that interaction. A reference marked not applicable needs a product reason; equal visual influence from all six is not a goal.

| Reference  | Source-level evidence                                                                                                                                                                                              | Best use for this product                                                             | Current Mirch boundary                                                                                                                          |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| Frappe CRM | [`ViewControls.vue`](https://github.com/frappe/crm/blob/2f6435d/frontend/src/components/ViewControls.vue) and [`Organization.vue`](https://github.com/frappe/crm/blob/2f6435d/frontend/src/pages/Organization.vue) | Responsive quick-filter controls and related records in the primary organization pane | `directory-list-view.tsx` groups search and columns; `organization-record-view.tsx` shows related records. Directory columns are not persisted. |
| Twenty     | [`ViewBar.tsx`](https://github.com/twentyhq/twenty/blob/211303ad/packages/twenty-front/src/modules/views/components/ViewBar.tsx)                                                                                   | Focused view picker, filter, sort, and options bar on object indexes                  | `DataTable.tsx` has a toolbar and URL-backed sorting. Directory view selection is absent.                                                       |
| Plane      | [`block.tsx`](https://github.com/makeplane/plane/blob/2f895b8/apps/space/components/issues/issue-layouts/list/block.tsx)                                                                                           | Compact task rows with a URL-backed contextual peek                                   | `task-navigation.ts` and task links carry panel state. Other task origins still need the stated close-path evidence.                            |
| Huly       | [`Activity.svelte`](https://github.com/hcengineering/platform/blob/63e28dc/plugins/activity-resources/src/components/Activity.svelte)                                                                              | Activity filtering, focused-message links, and scroll containment                     | `ActivityFeed.tsx` supplies chronology and actors. It has no message filter or focused-entry link.                                              |
| Corteza    | [`RecordListConfigurator.vue`](https://github.com/cortezaproject/corteza/blob/3835dfc/client/web/compose/src/components/PageBlocks/RecordListConfigurator.vue)                                                     | Field choice, wrapping, searchable fields, and list configuration                     | `DataTableViewOptions.tsx` changes visible columns only for the current mount; `settings/views` stores coarse presets.                          |
| Odoo       | [`project_task_views.xml`](https://github.com/odoo/odoo/blob/c50a4e0/addons/project/views/project_task_views.xml)                                                                                                  | Project, assignee, stage, and deadline facets for staff work                          | `tasks/page.tsx` shows assignees and project context. The comparable context filters are not in its table toolbar.                              |

Payload's [list controls](https://github.com/payloadcms/payload/blob/06f05f7/packages/ui/src/elements/ListControls/index.tsx) are an MIT-licensed admin reference for permission-aware actions and query presets, not the visual template for customer pages. Agentic Inbox remains the separate email reference. Frappe CRM, Twenty's application files, Plane, Huly, and Odoo are behavior or process references under their respective licenses. Corteza and Payload code reuse needs an exact-file license check and retained notices. No code from these references is copied by this contract.

## State matrix

Every surface must be checked in empty, loading, populated, long-content, error, unauthorized, keyboard, and narrow/mobile states. Stateful navigation belongs in the URL (`tab`, `view`, `page`, `panel`, `returnTo`) rather than referrer or history-length inference.

## Current guards

- `tests/e2e/customer/route-health.spec.ts` checks route health, identifier leakage, light-mode default, contextual task close, and canonical task rendering.
- `pnpm lint`, `pnpm typecheck`, `pnpm check:brand`, `pnpm check:docs`, and `git diff --check` are required before a UI batch is complete.
- A route-health pass alone is insufficient evidence for visual completion; add state assertions and screenshots when a surface changes.
- Before another reference-driven UI pass, name the exact source component, the observed interaction, the Mirch component to change, and the expected before/after state. Broad passes record relevance for all six product and workflow references. A generic product screenshot is not enough source evidence.

## Regression rule

If the same UX defect needs three corrective commits, add an automated test for it before continuing page-level work. Do not call a surface complete from a code-only review.
