---
wp: 'M0-W2'
attempt: 1
status: DONE
filesChanged:
  - .github/workflows/ci.yml
  - docs/orchestration/m0/reports/M0-W2.md
acceptance:
  - command: 'python3 -c "import yaml,sys; yaml.safe_load(open(''.github/workflows/ci.yml''))"'
    exitCode: 1
    evidence: "ModuleNotFoundError: No module named 'yaml'. PyYAML is not installed on this machine, so the brief's Ruby alternative below is the parse check of record."
  - command: 'ruby -ryaml -e "YAML.load_file(''.github/workflows/ci.yml'')"'
    exitCode: 0
    evidence: 'Parsed with no output. A structural dump of the parsed file showed on = {pull_request, push: {branches: [main]}}, permissions = {contents: read}, and the steps checkout@v4, pnpm/action-setup@v4, setup-node@v4 (node-version-file .nvmrc, cache pnpm), pnpm install --frozen-lockfile, pnpm exec playwright install --with-deps chromium, pnpm verify, upload-artifact@v4 with if failure().'
  - command: 'grep -n "secrets\." .github/workflows/ci.yml'
    exitCode: 1
    evidence: 'No matches (grep exits 1 when nothing matches). The workflow references no secrets.'
  - command: 'grep -n "uses:" .github/workflows/ci.yml'
    exitCode: 0
    evidence: 'Line 18 actions/checkout@v4, line 22 pnpm/action-setup@v4, line 25 actions/setup-node@v4, line 41 actions/upload-artifact@v4. Only the four official actions the brief allows, each pinned to a major version.'
  - command: 'prettier --check .github/workflows/ci.yml docs/orchestration/m0/reports/M0-W2.md'
    exitCode: 0
    evidence: 'All matched files use Prettier code style. Run with the main checkout binary (node_modules/.bin/prettier) against the repository .prettierrc.json, so pnpm format:check in pnpm verify will not fail on these files.'
testsAdded: []
choices:
  - 'pnpm/action-setup@v4 runs before actions/setup-node@v4. setup-node with cache: pnpm needs the pnpm binary on PATH to find the store, so the order matters.'
  - 'pnpm/action-setup has no version input, so it takes pnpm@10.34.5 from packageManager in package.json and cannot drift from the local toolchain.'
  - 'Kept the Playwright Chromium install step as dispatched, although pnpm verify (spec §6.6) does not run test:e2e today. It costs install time on every run until e2e joins CI. The lead may want to drop or gate it.'
  - 'The failure upload uses if-no-files-found: ignore, because a failure before pnpm test produces no coverage/, playwright-report/ or test-results/, and a missing path must not add a second error. retention-days: 7 keeps artifact storage small.'
  - 'Push triggers are limited to main. pull_request has no filter, so it runs for PRs into any base branch, including m0-bootstrap and the wp/* integration branches.'
openQuestions:
  - 'The workflow is not yet run on GitHub because no remote exists (Q-004). Green-run acceptance is still open until the lead pushes.'
  - 'playwright.config.ts uses reporter: list, which writes no playwright-report/ directory. Only test-results/ (retain-on-failure traces) is produced. If an HTML report is wanted in the artifact, the reporter config needs a change outside this WP scope.'
---

# Report: M0-W2

I added `.github/workflows/ci.yml`. It runs on every pull request and on pushes to `main`, with the token limited to `contents: read`.

The single `verify` job does the following, in order:

1. Checks out the repository.
2. Enables pnpm from the `packageManager` field.
3. Sets up Node from `.nvmrc` with the pnpm store cached.
4. Installs with `pnpm install --frozen-lockfile`.
5. Installs Playwright Chromium with its system dependencies.
6. Runs `pnpm verify`.

If any step fails, the job uploads `coverage/`, `playwright-report/` and `test-results/` as one artifact. The workflow has no secrets and no deploy steps.

The parse check of record is the Ruby one, because PyYAML is not installed on this machine. The Python command exited 1 on the missing module, not on the YAML.

What to look at first:

- Nothing in `pnpm verify` runs Playwright yet, so the Chromium install step currently only costs time.
- The current Playwright `list` reporter never writes `playwright-report/`, so that upload path will be empty.
