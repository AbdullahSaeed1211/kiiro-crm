---
wp: 'M0-W1'
attempt: 1
status: DONE
filesChanged:
  - 'scripts/check-brand.ts'
  - 'scripts/check-vocab.ts'
  - 'scripts/check-disables.ts'
  - 'scripts/check-docs.ts'
  - 'scripts/check-size.ts'
  - 'scripts/check-scope.ts'
  - 'scripts/lib/critical-paths.ts'
  - 'scripts/lib/files.ts'
  - 'scripts/lib/report.ts'
  - 'scripts/lib/scan.ts'
  - 'scripts/lib/workspace.ts'
  - 'scripts/test/checks/run-check.ts'
  - 'scripts/test/checks/brand.test.ts'
  - 'scripts/test/checks/vocab.test.ts'
  - 'scripts/test/checks/disables.test.ts'
  - 'scripts/test/checks/docs.test.ts'
  - 'scripts/test/checks/size.test.ts'
  - 'scripts/test/checks/scope.test.ts'
  - 'scripts/fixtures/checks/brand/apps/site/src/copy.ts'
  - 'scripts/fixtures/checks/brand/apps/web/cloudflare-env.d.ts'
  - 'scripts/fixtures/checks/brand/output/assets/strings.txt'
  - 'scripts/fixtures/checks/brand/packages/ui/src/label.ts'
  - 'scripts/fixtures/checks/brand/tenants/example.jsonc'
  - 'scripts/fixtures/checks/vocab/packages/kernel/src/clean.ts'
  - 'scripts/fixtures/checks/vocab/packages/kernel/src/records.ts'
  - 'scripts/fixtures/checks/vocab/packages/modules/crm/src/lead.ts'
  - 'scripts/fixtures/checks/vocab/packages/platform/src/stages.spec.ts'
  - 'scripts/fixtures/checks/vocab/packages/platform/src/stages.ts'
  - 'scripts/fixtures/checks/vocab/packages/platform/test/helpers.ts'
  - 'scripts/fixtures/checks/disables/apps/site/src/allowed.ts'
  - 'scripts/fixtures/checks/disables/packages/kernel/src/gated.ts'
  - 'scripts/fixtures/checks/disables/scripts/fixtures/nested.ts'
  - 'scripts/fixtures/checks/docs/apps/site/package.json'
  - 'scripts/fixtures/checks/docs/packages/kernel/README.md'
  - 'scripts/fixtures/checks/docs/packages/kernel/package.json'
  - 'scripts/fixtures/checks/docs/packages/kernel/src/index.ts'
  - 'scripts/fixtures/checks/docs/packages/modules/crm/README.md'
  - 'scripts/fixtures/checks/docs/packages/modules/crm/package.json'
  - 'scripts/fixtures/checks/docs/packages/modules/crm/src/index.ts'
  - 'scripts/fixtures/checks/docs/packages/templates/package.json'
  - 'scripts/fixtures/checks/docs/packages/ui/README.md'
  - 'scripts/fixtures/checks/docs/packages/ui/package.json'
  - 'scripts/fixtures/checks/docs/packages/ui/src/index.ts'
  - 'scripts/fixtures/checks/size/grown/build/worker.js'
  - 'scripts/fixtures/checks/size/grown/build/server-functions/default/handler.mjs'
  - 'scripts/fixtures/checks/size/grown/docs/reports/size.json'
  - 'scripts/fixtures/checks/scope/docs/orchestration/m0/plan.md'
  - 'scripts/fixtures/checks/scope/critical.txt'
  - 'scripts/fixtures/checks/scope/flagged.txt'
  - 'docs/orchestration/m0/reports/M0-W1.md'
acceptance:
  - command: 'pnpm vitest run scripts/test/checks'
    exitCode: 0
    evidence: 'Test Files  6 passed (6); Tests  38 passed (38)'
  - command: 'pnpm check:brand'
    exitCode: 0
    evidence: 'check:brand: ok'
  - command: 'pnpm check:vocab'
    exitCode: 0
    evidence: 'check:vocab: ok'
  - command: 'pnpm check:disables'
    exitCode: 0
    evidence: 'check:disables: ok'
  - command: 'pnpm check:docs'
    exitCode: 0
    evidence: 'check:docs: ok'
  - command: 'pnpm exec tsc --noEmit --strict --module esnext --moduleResolution bundler --target ES2023 --types node --skipLibCheck scripts/*.ts scripts/lib/*.ts'
    exitCode: 0
    evidence: 'No output. Also exit 0 with the remaining tooling/tsconfig/base.json flags (noUncheckedIndexedAccess, exactOptionalPropertyTypes, noPropertyAccessFromIndexSignature, verbatimModuleSyntax, isolatedModules) including scripts/test/checks/*.ts.'
  - command: 'pnpm check:brand --root scripts/fixtures/checks/brand'
    exitCode: 1
    evidence: 'apps/site/src/copy.ts:2 MIRCH / apps/site/src/copy.ts:3 globex studio / check:brand: 2 finding(s). With --path scripts/fixtures/checks/brand/output: scripts/fixtures/checks/brand/output/assets/strings.txt:1 Twenty, exit 1.'
  - command: 'pnpm check:vocab --root scripts/fixtures/checks/vocab'
    exitCode: 1
    evidence: 'packages/kernel/src/records.ts:2 Lead / :3 BOOKING / :4 tasks / packages/platform/src/stages.ts:1 patients / check:vocab: 4 finding(s)'
  - command: 'pnpm check:disables --root scripts/fixtures/checks/disables'
    exitCode: 1
    evidence: 'packages/kernel/src/gated.ts:1 complexity / :3 max-lines / :5 @typescript-eslint/no-explicit-any / check:disables: 3 finding(s)'
  - command: 'pnpm check:docs --root scripts/fixtures/checks/docs'
    exitCode: 1
    evidence: 'packages/kernel/README.md: missing "## Public API" / packages/kernel/src/index.ts:6, :11, :14 exported function lacks TSDoc / packages/modules/crm/README.md: missing "## Ports" and "## Invariants" / packages/templates: missing README.md / check:docs: 7 finding(s)'
  - command: 'pnpm size --root scripts/fixtures/checks/size/grown --build build'
    exitCode: 1
    evidence: 'check:size: 197 bytes is +97.0% over the committed 100 bytes (limit +20%); rerun with --allow-growth and cite an ADR'
  - command: 'pnpm check:scope M0-W1 --root scripts/fixtures/checks/scope --files scripts/fixtures/checks/scope/critical.txt'
    exitCode: 1
    evidence: 'FAIL package.json (critical path, lead-owned) / FAIL tooling/eslint/rules.js (critical path, lead-owned) / check:scope: M0-W1 3 changed, 2 failed, 0 flagged'
  - command: 'pnpm size'
    exitCode: 0
    evidence: 'check:size: no build output at apps/web/.open-next/worker.js; nothing to measure'
  - command: 'pnpm check:scope M0-W1 --base m0-bootstrap'
    exitCode: 0
    evidence: "Run on the code commit before this report was added: FLAG scripts/lib/{critical-paths,files,report,scan,workspace}.ts (outside write scope) / check:scope: M0-W1 49 changed, 0 failed, 5 flagged. The flags come from plan.md's M0-W1 row omitting scripts/lib/, which the brief includes."
  - command: 'pnpm exec prettier --check scripts'
    exitCode: 0
    evidence: 'All matched files use Prettier code style.'
  - command: 'pnpm exec eslint --no-config-lookup --config <scratchpad>/eslint.scripts.config.mjs scripts/check-*.ts scripts/lib/*.ts scripts/test/checks/*.ts'
    exitCode: 0
    evidence: "No problems. The scratch config is eslint.config.js's strictTypeChecked + stylisticTypeChecked + sonarjs recommended + gatedRules, with base.json as the project-service default project, because the repository config cannot parse scripts/ (see openQuestions)."
testsAdded:
  - 'scripts/test/checks/brand.test.ts'
  - 'scripts/test/checks/vocab.test.ts'
  - 'scripts/test/checks/disables.test.ts'
  - 'scripts/test/checks/docs.test.ts'
  - 'scripts/test/checks/size.test.ts'
  - 'scripts/test/checks/scope.test.ts'
choices:
  - 'Each check exports pure functions plus a CLI block guarded by isMain(import.meta.url); every check accepts --root <dir> (default cwd) so tests and planted fixtures can target a directory. Shared helpers live in scripts/lib (files, scan, report, workspace, critical-paths). Line counts: brand 49, vocab 33, disables 63, docs 66, size 76, scope 143.'
  - 'check:brand matches tokens case-insensitively as substrings, so mirchmedia or PlaneTakeoff are caught. Tenant names come from the displayName values in tenants/*.jsonc via a regex, avoiding a JSONC parser; slugs are not added. Binary files (NUL byte) are skipped. --path scans every file under the directory, including node_modules inside the build output.'
  - 'check:vocab splits camelCase, PascalCase and snake_case identifiers into words, then matches whole words with an optional plural, so createLead, BOOKING_STATUS and tasks are flagged while leadership, projection and dealers are not. Comments are scanned too. Only code extensions are read; tests are test/, tests/, __tests__/, *.test.* and *.spec.*.'
  - "check:disables loads the gated rule names at runtime by importing tooling/eslint/rules.js (keys of gatedRules and tsxOverrides). It handles line, block and multi-line block directives and strips '-- description'. A blanket eslint-disable that names no rule is not flagged, following the brief's 'name any gated rule'; the generated apps/web/cloudflare-env.d.ts and payload-types.ts use blanket disables. It scans top-level code files plus apps/, packages/ and scripts/, excluding scripts/fixtures/."
  - 'check:docs checks README headings only for workspace packages under packages/. apps/web has no README, and acceptance requires exit 0 on the repository; spec §6.5 and §7.3 speak of packages. Package directories come from the dir/* entries in pnpm-workspace.yaml and must hold a package.json, so packages/modules itself is skipped. TSDoc detection is line-based on prettier-formatted code: export function, export default function, and export const x = (...) => | function | <T>. A /** */ block must end on the line directly above.'
  - 'check:size adds --build <dir> (default apps/web/.open-next, resolved against --root) because .open-next is gitignored and cannot be committed as a fixture. The baseline is totalBytes in docs/reports/size.json under --root; no baseline passes. With --write, the record {files, totalBytes} in raw bytes is written only after the growth check passes.'
  - "check:scope finds columns by the header names WP, Owner and Write scope. Scope entries support a trailing '/', '*' and '**'. Critical paths fail only when the row's Owner is not Lead, so lead WPs such as M0-L1 are not failed. --files <list> reads changed paths from a file instead of git and drives the planted fixture. Without --base and with no upstream, the git error is printed and the exit code is 1."
  - 'The §0.7 critical-path list lives in scripts/lib/critical-paths.ts to keep check-scope.ts under 150 lines; any spec §0.7 change must be mirrored there.'
  - 'Two inline eslint-disable-next-line comments for sonarjs/no-os-command-from-path, with reasons, allow calling git from PATH (check-scope.ts and scope.test.ts). The rule is not gated, so check:disables accepts them.'
openQuestions:
  - "pnpm lint cannot parse scripts/**: no tsconfig includes scripts/, so typescript-eslint's projectService reports 'was not found by the project service' for every script and test. A lead-owned fix is needed, for example scripts/tsconfig.json extending tooling/tsconfig/base.json or allowDefaultProject in eslint.config.js."
  - "scripts/fixtures/** holds planted eslint-disable directives and brand/vocab tokens by design. Add scripts/fixtures/** to the eslint.config.js ignores so pnpm lint --max-warnings 0 does not report unused-directive warnings or fixture code, and consider adding it to jscpd and to knip's scripts/**/*.ts entry glob as well."
  - 'The M0-W1 row in docs/orchestration/m0/plan.md omits scripts/lib/, which the brief includes, so check:scope prints 5 FLAG lines for scripts/lib/*.'
  - 'check:docs skips apps/. To cover apps, add apps/web/README.md and widen the packages/ filter in checkDocs.'
  - "check:brand --path on real OpenNext output will probably hit third-party code, such as icon names containing 'plane' or locale strings containing 'twenty'. CI use in M1 may need an allowlist or a whole-word mode."
  - "Spec §3 also forbids 'Payload' in UI copy. check:brand does not enforce this: the brief does not list it, and code legitimately imports payload."
  - "check:size compares against the docs/reports/size.json in the working tree (the brief's 'committed value'), not main's copy (spec §6.5 'versus main')."
  - 'pnpm harness:record was not run because it does not exist in M0.'
---

# Report: M0-W1

All six repository checks are implemented under `scripts/`, with shared helpers in `scripts/lib/`, 38 Vitest tests in `scripts/test/checks/`, and planted fixtures in `scripts/fixtures/checks/`.

- Each planted fixture makes its check exit 1.
- `check:brand`, `check:vocab`, `check:disables` and `check:docs` exit 0 on the repository.
- `size` exits 0 with a notice when there is no build output.
- `check:scope M0-W1 --base m0-bootstrap` exits 0, flagging only `scripts/lib/*`.

Look at these first:

1. `check:docs` is limited to `packages/`, so that the repository passes without an `apps/web` README.
2. The repository ESLint config cannot lint `scripts/` until a tsconfig covers the directory.
3. `scripts/fixtures/**` should be added to the ESLint ignores.
