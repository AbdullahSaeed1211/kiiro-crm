---
{
  "wp": "M3-W2",
  "attempt": 3,
  "status": "DONE",
  "filesChanged": [
    "packages/modules/work/README.md",
    "packages/modules/work/src/index.ts",
    "packages/modules/work/src/ports/work.ts",
    "packages/modules/work/src/domain/rank.ts",
    "packages/modules/work/src/domain/rules.ts",
    "packages/modules/work/src/queries/my-tasks.ts",
    "packages/modules/work/src/commands/projects.ts",
    "packages/modules/work/src/commands/tasks.ts",
    "packages/modules/work/test/work.test.ts",
    "packages/ui/src/composites/CalendarMonth/CalendarMonth.tsx",
    "packages/ui/src/composites/CalendarMonth/index.ts",
    "packages/ui/src/composites/TaskSheet/TaskSheet.tsx",
    "packages/ui/src/composites/TaskSheet/index.ts",
    "apps/web/src/server/queries/work/read-models.ts",
    "apps/web/src/app/(app)/page.tsx",
    "apps/web/src/app/(app)/my-tasks/page.tsx",
    "apps/web/src/app/(app)/projects/page.tsx",
    "apps/web/src/app/(app)/projects/[id]/page.tsx",
    "apps/web/src/app/(app)/tasks/[id]/page.tsx",
    "apps/web/src/app/(app)/calendar/page.tsx",
    "harness/metrics/attempts/M3-W2-a1.json",
    "harness/metrics/attempts/M3-W2-a2.json",
    "harness/metrics/attempts/M3-W2-a3.json",
    "docs/orchestration/m3/reports/M3-W2.md"
  ],
  "acceptance": [
    {
      "command": "pnpm exec vitest run packages/modules/work/test apps/web/test/queries/work apps/web/test/actions/work packages/adapters/payload/test/repositories/task-repository.test.ts",
      "exitCode": 0,
      "evidence": "5 test files passed; 34 tests passed, including existing Payload task repository compare-and-set coverage and the new work invariant tests."
    },
    {
      "command": "pnpm typecheck",
      "exitCode": 0,
      "evidence": "All workspace TypeScript projects passed with no diagnostics."
    },
    {
      "command": "pnpm lint",
      "exitCode": 0,
      "evidence": "eslint . --max-warnings 0 passed with no problems."
    },
    {
      "command": "pnpm format:check",
      "exitCode": 0,
      "evidence": "All matched files use Prettier code style."
    },
    {
      "command": "pnpm --filter web build",
      "exitCode": 0,
      "evidence": "Next.js production build completed and listed /calendar, /my-tasks, /projects, /projects/[id], and /tasks/[id]."
    },
    {
      "command": "pnpm verify:fast",
      "exitCode": 0,
      "evidence": "format:check, typecheck, lint, and changed unit tests all passed."
    },
    {
      "command": "pnpm harness:record M3-W2 --attempt 2",
      "exitCode": 1,
      "evidence": "All functional gates passed. check:scope could not load the M3 row because the plan uses the header 'Exclusive write scope' while scripts/check-scope.ts requires a header named 'Write scope'; the recorder wrote harness/metrics/attempts/M3-W2-a2.json and suggested SCOPE_VIOLATION."
    },
    {
      "command": "pnpm verify",
      "exitCode": 0,
      "evidence": "Full repository verification passed: format, typecheck, lint, dependency graph, Knip, jscpd, brand/vocabulary/disable/docs checks, six harness eval targets, 609 unit tests, 17 integration tests, production build, and size gate. Existing coverage parsing warnings only exclude unrelated TSX files from the coverage map."
    },
    {
      "command": "pnpm harness:record M3-W2 --attempt 3",
      "exitCode": 1,
      "evidence": "The remediation gates passed after removing the dead rank export and reusing the legacy TaskPriority contract; check:scope still cannot load the M3 row because the plan header remains 'Exclusive write scope' instead of the parser-required 'Write scope'."
    }
  ],
  "testsAdded": [
    "packages/modules/work/test/work.test.ts: DST-aware My Tasks buckets, subtask depth/open-child rules, deterministic ranks, and assignment/completion filtering"
  ],
  "choices": [
    "Added a richer WorkRepository contract alongside the legacy TaskRepository so existing board/timeline consumers and adapter tests remain compatible while the complete project/task command surface has typed ports.",
    "Subtasks inherit the parent projectId, are capped at two ancestor levels, and cannot move a parent into done_success while a child remains open.",
    "Task updates and date writes use expectedUpdatedAt compare-and-set semantics; stage moves preserve the existing platform changeStage path and derive deterministic fractional ranks.",
    "The work read model queries Payload with overrideAccess false and the signed-in request, so project/task pages inherit the existing staff scope and report-chain access rules.",
    "CalendarMonth and TaskSheet are added as leaf composites without editing the central UI barrel; the lead can reconcile exports during assembly.",
    "The report is DONE for the implementation. The only failing harness gate is the lead-owned M3 plan/parser header mismatch, not a critical-path or changed-file scope violation.",
    "Remediation finding, CONTRACT_DRIFT and GATE_FAILURE: the new WorkTaskRecord introduced a second TaskPriority export and left ranksForOrder unused, so the full Knip gate reported one dead legacy export and one dead new export. Countermeasure: reuse the legacy TaskPriority type through the richer contract and remove the unused helper. Evidence: Knip, full verify, and attempt 3 passed every available gate; the append-only attempt retains the scope parser failure."
  ],
  "openQuestions": [
    "Lead should rename or broaden the M3 plan table header so scripts/check-scope.ts can parse M3 rows; rerun harness:record after that plan-only correction.",
    "The richer WorkRepository adapter implementation and central package exports remain lead assembly work because the worker protocol prohibits central barrel and registration edits."
  ],
  "dependencyRequests": [
    "M3-L1: update docs/orchestration/m3/plan.md's write-scope header to the exact parser-recognized 'Write scope' label, then rerun check:scope and harness telemetry."
  ]
}
---

# Report: M3-W2

Implemented the work-management vertical across the module, work-specific UI composites, scoped read model, and product routes.

The module now exposes project/task records and commands, deterministic rank helpers, subtask invariants, and DST-aware My Tasks bucketing. The UI covers the dashboard, My Tasks, projects, project tabs, task record/sheet, and calendar. Existing board/timeline repository behavior remains intact, including compare-and-set protection.

All functional verification gates pass. `check:scope` is currently blocked by the M3 plan’s non-parser-recognized `Exclusive write scope` column header; no lead-owned critical path was changed.
