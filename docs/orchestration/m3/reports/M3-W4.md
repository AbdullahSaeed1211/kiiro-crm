---
{
  "wp": "M3-W4",
  "attempt": 4,
  "status": "DONE",
  "filesChanged": [
    "packages/modules/intake/",
    "packages/modules/mail/",
    "packages/adapters/cloudflare/src/intake/",
    "packages/adapters/cloudflare/src/mail/",
    "packages/adapters/cloudflare/src/cron/",
    "packages/adapters/cloudflare/src/index.ts",
    "packages/adapters/cloudflare/test/intake/",
    "packages/adapters/cloudflare/test/mail/resend-mail-sender.test.ts",
    "packages/adapters/cloudflare/test/cron/jobs.test.ts",
    "packages/modules/mail/test/domain.test.ts",
    "harness/metrics/attempts/M3-W4-a1.json",
    "harness/metrics/attempts/M3-W4-a2.json",
    "harness/metrics/attempts/M3-W4-a3.json",
    "harness/metrics/attempts/M3-W4-a4.json",
    "docs/orchestration/m3/reports/M3-W4.md"
  ],
  "acceptance": [
    {
      "command": "pnpm harness:record M3-W4 --attempt 4 --base origin/main --runner worker",
      "exitCode": 0,
      "evidence": "Format, typecheck, lint, changed unit tests, and check:scope all passed. Scope reported seven prose-pattern flags and zero critical failures; the flags are documented as plan-scope parser limitations."
    },
    {
      "command": "pnpm exec vitest run --project unit",
      "exitCode": 0,
      "evidence": "74 test files and 627 tests passed, including the intake HTTP/security matrix, rate-limit binding contract, Turnstile action/timeout tests, MIME parser and inbound retry tests, Resend timeout, and six-job continuation fixtures."
    },
    {
      "command": "pnpm check:docs",
      "exitCode": 0,
      "evidence": "Exported API and package documentation checks passed."
    },
    {
      "command": "pnpm check:scope M3-W4 --base origin/main",
      "exitCode": 0,
      "evidence": "No critical-path failures. Seven adapter paths are flagged because the plan stores prose descriptions such as 'intake and mail areas under packages/adapters/cloudflare/' rather than machine-matchable patterns."
    }
  ],
  "testsAdded": [
    "HTTP intake tests: exact CORS reflection, preflight, disallowed origins, methods, content types, bounded bodies, and server-key behavior.",
    "Turnstile and rate-limit adapter tests: expected action, hostname, timeout, and Cloudflare limit({ key }) contract.",
    "Mail tests: encoded/display-name MIME parsing, multipart transfer decoding, full-raw SHA-256 IDs, intake aliases, and recovery after activity/notification failure.",
    "Job tests: tenant-local midnight/09:00 windows, daily digest timing, duplicate windows, and cursor continuation beyond 200 rows."
  ],
  "choices": [
    "Kept adapter and module boundaries explicit: inbound intake calls an InboundIntakePort, while the lead-owned composition root supplies the port backed by submitIntake(channel: email).",
    "Used exact-origin CORS reflection with Vary: Origin, bounded stream reads before parsing, POST/OPTIONS-only handling, and JSON or form-urlencoded content types.",
    "Turnstile sends and validates an explicit action, preserves a ten-second timeout, bounds tokens, and logs only safe error names.",
    "Replaced the leading-header fallback with a real SHA-256 digest of the complete raw message and added MIME header/address/body/attachment decoding with injection remaining available for PostalMime.",
    "Added idempotent inbound activity and notification ports so duplicate message retries complete missing side effects.",
    "Persisted per-job cursors and computed tenant-local midnight and 09:00 boundaries with DST-safe calendar conversion.",
    "Recorded the repeat isolation breach and removed only the two exact accidental lead-checkout test files; no lead-owned source or plan files were changed."
  ],
  "openQuestions": [
    "Lead assembly remains required for Payload intake/mail repositories, jobRuns cursor persistence, public/internal routes, settings UI, Worker/mail-router entry, migration, generated types, and central seed wiring.",
    "The plan's prose scope entries still produce non-failing check:scope flags for adapter paths; the corrected header makes the gate executable but the prose patterns should be normalized in a lead-owned plan follow-up.",
    "The append-only attempt records preserve the review-confirmed GATE_FAILURE, MISSING_TEST, CONTRACT_DRIFT, EDGE_CASE, and SCOPE_VIOLATION classes; attempt 4's required gates are green."
  ],
  "dependencyRequests": [
    "Lead: wire the explicit InboundIntakePort to submitIntake with channel=email and tenant Payload repositories.",
    "Lead: implement the JobRunStore cursor read/write adapter and cursor-aware source queries in the central Payload assembly.",
    "Lead: compose the central HTTP/Worker/settings integrations and normalize plan scope patterns."
  ]
}
---

# M3-W4 report

Attempt 4 remediates the review findings across intake HTTP security, Cloudflare contracts, Turnstile action binding, MIME parsing, inbound intake routing, idempotent mail side effects, Resend timeout handling, tenant-local scheduling, and cursor continuation. The branch is locally verified and stops at the documented lead-owned integration boundaries.
