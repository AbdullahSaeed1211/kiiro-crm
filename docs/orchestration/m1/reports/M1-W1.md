---
{
  "wp": "M1-W1",
  "attempt": 1,
  "status": "DONE",
  "filesChanged": [
    "packages/adapters/payload/src/collections/activity.ts",
    "packages/adapters/payload/src/collections/attachments.ts",
    "packages/adapters/payload/src/collections/email-messages.ts",
    "packages/adapters/payload/src/collections/fields.ts",
    "packages/adapters/payload/src/collections/groups.ts",
    "packages/adapters/payload/src/collections/index.ts",
    "packages/adapters/payload/src/collections/job-runs.ts",
    "packages/adapters/payload/src/collections/notifications.ts",
    "packages/adapters/payload/src/collections/organizations.ts",
    "packages/adapters/payload/src/collections/projects.ts",
    "packages/adapters/payload/src/collections/settings.ts",
    "packages/adapters/payload/src/collections/tasks.ts",
    "packages/adapters/payload/src/collections/users.ts",
    "packages/adapters/payload/src/collections/values.ts",
    "packages/adapters/payload/src/collections/workflow-rules.ts",
    "packages/adapters/payload/src/collections/workflows.ts",
    "packages/adapters/payload/test/collections/collections.test.ts",
    "packages/adapters/payload/test/collections/field-validators.test.ts",
    "packages/adapters/payload/test/collections/workflow-rules.test.ts",
    "docs/orchestration/m1/reports/M1-W1.md",
    "harness/metrics/attempts/M1-W1-a1.json"
  ],
  "acceptance": [
    {
      "command": "pnpm exec vitest run packages/adapters/payload",
      "exitCode": 0,
      "evidence": "Test Files  6 passed (6); Tests  80 passed (80). The collection test sanitizes a config built from [...spikeCollections] and settingsGlobal and checks all 11 COLLECTIONS slugs plus SETTINGS_GLOBAL, every contract field with type and relationTo and hasMany, read/create/update/delete identity with SPIKE_ACCESS, SETTINGS_ACCESS, access.admin === canUseAdmin, the D-38 auth values, admin groups, indexes and the upload MIME list."
    },
    {
      "command": "pnpm typecheck",
      "exitCode": 0,
      "evidence": "pnpm -r --parallel exec tsc --noEmit printed no diagnostics."
    },
    {
      "command": "pnpm lint",
      "exitCode": 0,
      "evidence": "eslint . --max-warnings 0 printed no problems."
    },
    {
      "command": "pnpm format:check",
      "exitCode": 0,
      "evidence": "All matched files use Prettier code style! (after pnpm exec prettier --write on both directories)"
    },
    {
      "command": "pnpm check:brand",
      "exitCode": 0,
      "evidence": "check:brand: ok. The first run flagged workflow-rules.test.ts:34 twenty (a test title); renamed."
    },
    {
      "command": "pnpm check:docs",
      "exitCode": 0,
      "evidence": "check:docs: ok"
    },
    {
      "command": "pnpm depcruise",
      "exitCode": 0,
      "evidence": "no dependency violations found (136 modules, 194 dependencies cruised)"
    },
    {
      "command": "pnpm jscpd",
      "exitCode": 0,
      "evidence": "typescript: 81 files, 0 clones. Total 13 clones, 238 lines (1.33%), 964 tokens (0.94%), all in yaml and css outside the write scope."
    },
    {
      "command": "pnpm knip",
      "exitCode": 1,
      "evidence": "Zero findings inside the write scope. Remaining: Unused dependencies (6) lucide-react, @ops/platform (cloudflare adapter), @atlaskit/pragmatic-drag-and-drop, @atlaskit/pragmatic-drag-and-drop-hitbox, @svar-ui/react-gantt, @tanstack/react-table; Configuration hints (2). Identical to the run before any change. The first run after the change listed 18 unused exports and 1 unused type in src/collections; the barrel now exports only what is consumed."
    },
    {
      "command": "pnpm check:scope M1-W1 --base m1-spike",
      "exitCode": 0,
      "evidence": "check:scope: M1-W1 19 changed, 0 failed, 0 flagged (run on the code commit 48b8171)"
    },
    {
      "command": "pnpm harness:record M1-W1 --attempt 1 --base m1-spike",
      "exitCode": 0,
      "evidence": "PASS pnpm typecheck; PASS pnpm lint; PASS format:check; PASS typecheck; PASS lint; PASS vitest run --changed; PASS check:scope; suggestedClasses: none; wrote harness/metrics/attempts/M1-W1-a1.json"
    }
  ],
  "testsAdded": [
    "packages/adapters/payload/test/collections/collections.test.ts",
    "packages/adapters/payload/test/collections/workflow-rules.test.ts",
    "packages/adapters/payload/test/collections/field-validators.test.ts"
  ],
  "choices": [
    "Layout: one file per collection, shared helpers in fields.ts (spikeCollection, textField, selectOf, relationshipTo, hasManyTo, epochMs, countField, jsonField, stringListField, customDataField, recordReference, stageFields), select values in values.ts typed with satisfies against platform Role, StageCategory, StageColor and NotificationType, and pure workflow validators in workflow-rules.ts. The largest file is fields.ts at 134 lines.",
    "spikeCollection sets timestamps: true and versions: false, and spreads a copy of SPIKE_ACCESS[slug] so the sanitizer never mutates the shared access objects; the functions themselves stay identical (the test asserts toBe).",
    "spikeCollections is typed readonly CollectionConfig[] because sanitizeConfig appends its internal collections to the array it receives; the first test run showed the exported array grow from 11 to 15 entries. Callers pass [...spikeCollections].",
    "The config test calls sanitizeConfig (what buildConfig runs after plugins) with a stub db whose init throws; sanitizing never calls it.",
    "users: the auth block is copied from apps/web/src/collections/Users.ts (secure only when NODE_ENV is production). Also added name (required, max 120, useAsTitle), role (select, required, indexed, default staff), active (checkbox, default true, indexed), groups (has many) and reportsTo.",
    "Stage ids: the array has its own id text field with defaultValue crypto.randomUUID() and a beforeValidate hook (ensureStageId). Payload runs a field's validate after that field's beforeChange hooks but before it traverses array rows, so an id created in beforeChange would not exist yet when defaultStageId validates. Workflows set disableDuplicate: true, because a copy would keep the stage ids that records point at.",
    "A custom validate replaces Payload's built-in validation for that field, so validateStages enforces the 20-stage cap itself (maxRows: 20 stays for the admin UI) and validateDefaultStage handles a missing value. Stage names compare trimmed and case-insensitively.",
    "Compound indexes use collection indexes (CompoundIndex { fields, unique? } in Payload 3.89): tasks (project, stageId, rank) and (relatedType, relatedId); activity and emailMessages (recordType, recordId, occurredAt); attachments (recordType, recordId); notifications (user, readAt, createdAt); jobRuns (job, windowStart) unique. In @payloadcms/drizzle 3.89, build.js looks index columns up by field path, and traverseFields.js keys a single relationship column by field name (SQL name <name>_id), so relationship fields can take part. No migration was generated here.",
    "Single-field indexes: users role and active; organizations name and owner; projects organization, owner and stageId; tasks parentTask, stageId and dueAt; workflows recordType. Unique: groups name, notifications dedupeKey, emailMessages messageId, and users email from auth. Payload indexes createdAt and updatedAt automatically.",
    "Record-type selects (workflows recordType, recordType, relatedType) use Object.values(RECORD_TYPES): organization, project, task.",
    "attachments: upload.mimeTypes holds the 11 types of spec 9.7 plus application/x-zip-compressed, which some browsers report for zip files. crop and focalPoint are false. No storage adapter or size limit is set. fileName keeps the original display name, separate from Payload's built-in filename.",
    "emailMessages: to and cc are JSON with defaultValue [] and a string-list validator. Added an attachments has-many relationship for spec 10.4 attachmentIds, which the brief did not list. textBody maxLength 200000 counts characters, while the spec says 200 KB.",
    "jobRuns: status is running | completed | failed (default running), because spec 13 names no states; counters have min 0.",
    "settings: timezone defaults to UTC and must be a time zone Intl accepts; currency defaults to USD and must match ^[A-Z]{3}$; weekStartsOn must be 0 or 1; brand.primaryHex is empty or #rrggbb; radius defaults to md; the four modules checkboxes default to true; stalledDays defaults to 14 (D-33) within 1-365; senderStatus defaults to unverified. logoFileKey, faviconFileKey and terminology are left out, as they are not in the brief's subset.",
    "tasks: description reuses the 20000-character project limit, because spec 10.2 gives no task limit; rank is text up to 64 characters.",
    "Admin groups follow spec 17.13: People (users, groups), Records (organizations, projects, tasks), Configuration (workflows, settings), System (activity, attachments, notifications, emailMessages, jobRuns). No UI copy says Payload."
  ],
  "openQuestions": [
    "Exports to add to packages/adapters/payload/src/index.ts: export { ADMIN_GROUPS, settingsGlobal, spikeCollections } from './collections'. In apps/web/src/payload.config.ts: collections: [...spikeCollections], globals: [settingsGlobal], admin.user: COLLECTIONS.users. Then delete apps/web/src/collections/Users.ts, whose auth settings now live in the adapter. R2 storage goes on COLLECTIONS.attachments.",
    "Base commit: the worktree started on commit 2f2b9ff instead of 2744ae7. Branch wp/M1-W1 was reset to 2744ae7 before any work, and git log confirmed 2744ae7.",
    "users.name is required. Existing local user rows have no name, so seeding and provisioning must set one, and those rows fail validation on their next save.",
    "Risk, not verified: apps/web/src/payload-types.ts augments the payload module, so CollectionSlug narrows to the generated slugs. If apps/web imports the barrel before payload generate:types runs, relationTo values such as groups may fail its typecheck until types are regenerated.",
    "The harness gate vitest run --changed ran after the code was committed, so it probably selected no tests. The test evidence is the explicit pnpm exec vitest run packages/adapters/payload.",
    "pnpm knip exits 1 on findings that predate this package and lie outside its scope (6 unused dependencies, 2 configuration hints).",
    "Not implemented here: spec 11.1 field-level limits (staff read only name, email, avatar and role of users; staff edit only their own name and avatar), and moving records off a deleted stage (moveRecordsToStageId). validateDefaultStage does reject saving a workflow whose default stage was removed."
  ]
}
---

# Report: M1-W1

The spike collections and the settings global are in `packages/adapters/payload/src/collections/`, each wired to the lead's access functions. There are 80 passing tests in the adapter package, 52 of them in the three new test files. Every gate passes except `pnpm knip`, whose only findings predate this work and are outside its scope.

Look at these first:

1. `spikeCollections` is read-only: Payload appends its own collections to the array it is given, so pass `[...spikeCollections]` in `payload.config.ts`.
2. Stage ids are generated in a `beforeValidate` hook so the default-stage validator can see them; workflows cannot be duplicated.
3. `users.name` is now required, so the M1-L3 migration and seeding must set it.
