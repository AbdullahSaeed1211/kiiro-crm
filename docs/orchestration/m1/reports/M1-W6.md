---
{
  "wp": "M1-W6",
  "attempt": 1,
  "status": "DONE",
  "filesChanged": [
    "packages/adapters/payload/test/spike/local-stack.ts",
    "packages/adapters/payload/test/spike/local-d1.test.ts",
    "packages/adapters/payload/test/spike/scope.ts",
    "packages/adapters/payload/test/spike/conflict.ts",
    "packages/adapters/payload/test/spike/duplicates.ts",
    "tests/e2e/spike/smoke.spec.ts",
    "tests/e2e/spike/tsconfig.json",
    "docs/orchestration/m1/reports/M1-W6.md",
    "harness/metrics/attempts/M1-W6-a1.json"
  ],
  "acceptance": [
    {
      "command": "pnpm exec vitest run --project unit packages/adapters/payload/test/spike",
      "exitCode": 1,
      "evidence": "Test Files 1 failed (1); Tests 1 failed | 16 passed (17); Duration 6.03s (tests 5.59s); wall clock 6 s. Stack start (fresh D1 copy, migrate, seed) took 3.9 s in a probe run. The only failure is the product bug in openQuestions 1: 'two concurrent saveDates with the same expectedUpdatedAt: exactly one succeeds' fails with AssertionError: expected [ { …(10) }, { …(10) } ] to have a length of 1 but got 2. Passing: 7 scope cases (owner, manager, staff1, staff2, staff2 hidden tasks, seeded reports, indirect report), 3 conflict cases (changeStage CONFLICT, saveStage refused, saveDates refused), 3 cron cases (rerun, concurrent runs, D1 unique error shape) and 3 inbound email cases (repeat, missing Message-ID, concurrent)."
    },
    {
      "command": "pnpm test",
      "exitCode": 1,
      "evidence": "Test Files 1 failed | 50 passed (51); Tests 1 failed | 417 passed (418); Duration 8.16s. The spike file accounts for about 6 s. The only failure is the concurrent saveDates test above."
    },
    {
      "command": "E2E_BASE_URL=http://localhost:3001 pnpm test:e2e --grep spike",
      "exitCode": 0,
      "evidence": "Run against PAYLOAD_SECRET=dev-only-replace-with-32-random-bytes pnpm dev after pnpm db:reset:local && pnpm seed:dev. Port 3001 was used because another process held 3000 when the server started. Result: 7 passed, 1 skipped (7.6s); wall clock 9 s. desktop: cron 200/401, /tasks table, /tasks/board drag plus reload, /timeline drag plus reload. mobile: cron, /tasks table, /tasks/board 'Move to…' plus reload; /timeline skipped (mouse drag only). The WebKit browser for the iPhone 13 device was installed first with pnpm exec playwright install webkit."
    },
    {
      "command": "pnpm typecheck",
      "exitCode": 0,
      "evidence": "No diagnostics. It covers packages/adapters/payload/test/spike through the adapter tsconfig."
    },
    {
      "command": "pnpm exec tsc --noEmit -p tests/e2e/spike/tsconfig.json",
      "exitCode": 0,
      "evidence": "No diagnostics. pnpm typecheck does not cover tests/ (see openQuestions)."
    },
    { "command": "pnpm lint", "exitCode": 0, "evidence": "eslint . --max-warnings 0 printed no problems." },
    {
      "command": "pnpm format:check",
      "exitCode": 0,
      "evidence": "All matched files use Prettier code style! (after prettier --write on both directories)"
    },
    { "command": "pnpm check:brand", "exitCode": 0, "evidence": "check:brand: ok" },
    {
      "command": "pnpm depcruise",
      "exitCode": 0,
      "evidence": "no dependency violations found (256 modules, 517 dependencies cruised)"
    },
    {
      "command": "pnpm knip",
      "exitCode": 1,
      "evidence": "Unused exported types (1): TaskDatesInput apps/web/src/server/work/task-repository.ts:5:15. Nothing in this WP's scope."
    },
    {
      "command": "pnpm check:disables && pnpm jscpd",
      "exitCode": 0,
      "evidence": "check:disables exit 0. jscpd exit 0; its TypeScript and TSX clones are apps/web/src/app/(app)/tasks/board/page.tsx vs tasks/page.tsx and api/v1/internal/cron/route.ts vs email/inbound/route.ts, none in this scope."
    },
    {
      "command": "for i in 1 2 3 4 5 6; do curl -s -o /dev/null -w '%{http_code}\\n' -X POST http://localhost:3001/api/users/login -H 'content-type: application/json' -d '{\"email\":\"owner@example.test\",\"password\":\"DevPassword123!\"}' & done; wait",
      "exitCode": 0,
      "evidence": "200 500 200 500 500 500. Dev log for each 500: DrizzleQueryError 'Failed query: insert into \"users_sessions\" …' caused by 'D1_ERROR: UNIQUE constraint failed: users_sessions.id: SQLITE_CONSTRAINT (extended: SQLITE_CONSTRAINT_PRIMARYKEY)' at loginOperation (payload/dist/auth/operations/login.js:222:25). In an earlier Playwright run, POST /api/users/login returned 200 and the next GET /timeline returned 307 to /admin/login."
    },
    {
      "command": "pnpm harness:record M1-W6 --attempt 1 --base 2d39a8e",
      "exitCode": 1,
      "evidence": "Run on code commit 9523bfc with pnpm dev on :3000. FAIL pnpm test (exit 1, 7900 ms: the concurrent saveDates test); PASS pnpm test:e2e --grep spike (13120 ms); PASS format:check, typecheck, lint, vitest run --changed, check:scope; suggestedClasses: MISSING_TEST; wrote harness/metrics/attempts/M1-W6-a1.json."
    }
  ],
  "testsAdded": ["packages/adapters/payload/test/spike/local-d1.test.ts", "tests/e2e/spike/smoke.spec.ts"],
  "choices": [
    "Isolated D1: local-stack.ts copies apps/web/wrangler.jsonc and .dev.vars into apps/web/.wrangler/spike-test (git-ignored, inside this worktree) and changes the working directory there. getPlatformProxy keeps state next to the config it finds, so the tests get their own D1 file. pnpm test therefore never resets the development database or contends with a running pnpm dev. Setup removes the directory, starts Payload with apps/web/src/payload.config.ts, runs payload.db.migrate({ migrations }) from apps/web/src/migrations/index.ts and runs seedAll from scripts/seed/steps.ts. dispose restores the working directory.",
    "One test file (local-d1.test.ts) registers case lists exported by scope.ts, conflict.ts and duplicates.ts. All groups share one Payload instance and one D1 file, and order matters: scope checks run on the rows as seeded. Separate files would each start their own proxy.",
    "Requests per user are createLocalReq({ user: { ...doc, collection: 'users' } }, payload), as getWorkDeps builds them from a session. Tasks are read through createTaskRepository(req).listTasks() and through payload.find with overrideAccess false; projects and organizations through payload.find with overrideAccess false.",
    "Expected staff scope is hard-coded from scripts/seed/data.ts. staff1 sees 7 tasks (assigned, Design group, members of the website project) and 1 project. staff2 sees 5 tasks and no projects. Neither sees the organization, which the manager owns. staff2's repository loadRecord returns undefined for every task only staff1 may see.",
    "Transitive reports: nobody reports to a staff user in the seed, so the isolated database adds spike-report (reports to staff1) and spike-indirect-report (reports to spike-report), plus a project owned by the indirect report. staff1's actor has 2 report ids and sees that project; staff2 does not. Tasks have no owner field, so report scope is shown on projects. loadReportIds(manager) returns staff1 and staff2; manager actors carry no report ids.",
    "Conflicts: changeStage moves a task and a second changeStage with the pre-move version returns CONFLICT; the task row and activity count are unchanged. saveStage called directly with a stale version returns undefined and writes nothing (database guard). saveDates with a stale version returns undefined, and with the fresh version saves. Dates go to +90 days (and +120 days for the race test), outside the cron windows.",
    "Cron: the Cloudflare adapter is loaded with a runtime import of packages/adapters/cloudflare/src/index.ts and typed locally, because the boundaries lint rule forbids adapter-to-adapter imports; the composition mirrors apps/web/src/app/api/v1/internal/cron/route.ts. The window at seed + 12 h holds one open task for staff2: the first run creates 1 notification, the rerun creates 0 (logged created 0, skipped 1). The window at seed + 36 h is run twice concurrently and creates 1 notification. The time zone comes from the settings global.",
    "Inbound email: accepting the same raw message twice stores one row. So does a message without Message-ID (stored with a sha256: id), and so do two concurrent accepts.",
    "Playwright selectors use page content only (the coordinator noted that m1-spike moved the shell into the layout): table columnheader, section regions named by stage, [data-card-id], .wx-bar and the .wx-row cells [data-col-id=':start'] / [data-col-id=':end']. Desktop drags 'Plan launch checklist'; the phone project moves 'Schedule kickoff meeting' through 'Move to…' so the two projects never touch the same card. The timeline drag moves 'Design style guide' 88 px (2 days), leftwards when the bar is near the right edge, so the specs re-run without a reset.",
    "Owner sign-ins are serialized with a mkdir lock at os.tmpdir()/ops-spike-sign-in.lock, stale after 60 s, because concurrent sign-ins of one user break on D1 (openQuestions 2). DEV_PASSWORD and the internal secret come from scripts/seed/data.ts and apps/web/.dev.vars(.example), not literals.",
    "tests/e2e/spike/tsconfig.json extends tooling/tsconfig/base.json, so ESLint's project service and tsc can type the spec; the root tsconfig does not include tests/.",
    "Local-only files: apps/web/.dev.vars copied from .dev.vars.example (git-ignored). next dev rewrote apps/web/next-env.d.ts and it was restored each time. Nothing touched Cloudflare; PAYLOAD_REMOTE_BINDINGS and CLOUDFLARE_ENV were never set."
  ],
  "openQuestions": [
    "Product bug (D-36), the failing test in conflict.ts (expectOneConcurrentWriteWins). Two concurrent saveDates calls with the same expectedUpdatedAt should give one saved record and one undefined. Actual: both return the saved document and the first write is lost; 5 of 5 probe runs and every test run showed it. Cause: updateIfUnchanged in packages/adapters/payload/src/repositories/local-api.ts (update by where at line 62) uses Payload's update by where, which selects matching ids and then writes each by id, so both writes pass the updatedAt guard. saveStage uses the same function, so concurrent board moves are affected too. Options: a conditional UPDATE … WHERE updated_at = ? through payload.db that checks rows affected, or accept the window and mark the test it.fails.",
    "Product bug: concurrent sign-ins of one user on D1. Six parallel POST /api/users/login for the owner returned 2 × 200 and 4 × 500 (UNIQUE constraint failed: users_sessions.id, payload/dist/auth/operations/login.js:222). A login that returned 200 can also lose its session: the next GET /timeline returned 307 to /admin/login. Payload rewrites the user's whole sessions array (all users_sessions rows) on each login, without a transaction on D1. This affects one user signing in from two tabs or devices at once. The auth settings live in packages/adapters/payload/src/collections/users.ts; whether Payload 3.89 can turn off server-side sessions there needs checking.",
    "M1-W8 question answered: on local D1 a unique violation reaches the adapter as DrizzleQueryError('Failed query: insert into \"notifications\" …'), caused by Error('D1_ERROR: UNIQUE constraint failed: notifications.dedupe_key: SQLITE_CONSTRAINT (extended: SQLITE_CONSTRAINT_UNIQUE)'). It has no code and is not a ValidationError. insertUnique's cause-message check handles it; the concurrent cron and inbound tests each stored one row.",
    "pnpm test now starts a local Wrangler proxy and D1 (no network) and adds about 6 s. Recommended vitest.config.ts split: in 'unit' add exclude: [...configDefaults.exclude, 'packages/**/test/spike/**']; add a project { test: { name: 'integration', environment: 'node', include: ['packages/**/test/spike/**/*.test.ts'], hookTimeout: 180_000, fileParallelism: false } } and a script test:integration: vitest run --project integration.",
    "Recommended playwright.config.ts: webServer { command: 'pnpm db:reset:local && pnpm seed:dev && pnpm dev', url: 'http://localhost:3000/api/v1/health', env: { PAYLOAD_SECRET: 'dev-only-replace-with-32-random-bytes' }, reuseExistingServer: !process.env.CI, timeout: 180_000 }, because next dev does not load .dev.vars into process.env. Also a setup project that signs in once and shares storageState, which would replace the spec's sign-in lock. The mobile project (iPhone 13) needs pnpm exec playwright install webkit.",
    "tests/ is outside the root tsconfig include and outside pnpm typecheck (pnpm -r skips the root). Add tests/e2e to a checked tsconfig, or keep per-directory tsconfigs and add them to typecheck.",
    "At base 2d39a8e, /tasks still renders fixture rows (apps/web/src/server/queries/work/tasks/listTasks.ts) and does not require a session, so the table smoke check does not show Payload data. Board and timeline do use getWorkDeps.",
    "harness:record suggests MISSING_TEST only because of the intentionally failing concurrent-write test. The lead decides whether it stays red, becomes it.fails or is fixed before merge.",
    "pnpm test prints 'Failed to parse … Excluding it from coverage' for packages/ui .tsx files; these are coverage warnings, not failures, and not caused by this WP.",
    "knip reports TaskDatesInput in apps/web/src/server/work/task-repository.ts, outside this scope."
  ]
}
---

# Report: M1-W6

Spike evidence on local D1 and `pnpm dev`, commit 9523bfc on `wp/M1-W6`.

- **Integration tests:** 16 of 17 pass in about 6 s. They cover staff scope for all four roles plus two levels of reports, stale-version conflicts through `changeStage`, `saveStage` and `saveDates`, repeated and concurrent due-soon cron runs, and repeated and concurrent inbound email. Each creates exactly one row.
- **Playwright smoke:** 7 passed and 1 skipped. It covers sign-in through `/admin/login`, `/tasks`, a board move that survives a reload, a timeline drag that survives a reload, and the cron route returning 200 with the dev secret and 401 with a wrong one.

Look at these first:

1. Concurrent compare-and-set loses updates on D1. Both writes succeed, and the test is left failing (open question 1, D-36).
2. Concurrent sign-ins of one user return 500 or lose the session on D1 (open question 2). The spec serializes sign-ins.
3. The integration tests run on their own D1 copy, but they make `pnpm test` depend on the local Wrangler proxy. A Vitest project split and a Playwright `webServer` entry are proposed in open questions 4 and 5.
