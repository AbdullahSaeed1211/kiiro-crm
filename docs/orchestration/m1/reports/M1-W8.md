---
{
  "wp": "M1-W8",
  "attempt": 1,
  "status": "DONE",
  "filesChanged": [
    "packages/adapters/payload/src/repositories/documents.ts",
    "packages/adapters/payload/src/repositories/due-item-source.ts",
    "packages/adapters/payload/src/repositories/email-headers.ts",
    "packages/adapters/payload/src/repositories/email-message-sink.ts",
    "packages/adapters/payload/src/repositories/index.ts",
    "packages/adapters/payload/src/repositories/local-api.ts",
    "packages/adapters/payload/src/repositories/notification-store.ts",
    "packages/adapters/payload/src/repositories/task-mapping.ts",
    "packages/adapters/payload/src/repositories/task-repository.ts",
    "packages/adapters/payload/src/repositories/unique-insert.ts",
    "packages/adapters/payload/src/repositories/workflow-mapping.ts",
    "packages/adapters/payload/test/repositories/due-item-source.test.ts",
    "packages/adapters/payload/test/repositories/email-message-sink.test.ts",
    "packages/adapters/payload/test/repositories/fake-payload.ts",
    "packages/adapters/payload/test/repositories/notification-store.test.ts",
    "packages/adapters/payload/test/repositories/task-repository.test.ts",
    "docs/orchestration/m1/reports/M1-W8.md",
    "harness/metrics/attempts/M1-W8-a1.json"
  ],
  "acceptance": [
    {
      "command": "pnpm exec vitest run packages/adapters/payload",
      "exitCode": 0,
      "evidence": "Test Files  10 passed (10); Tests  101 passed (101). The 21 new tests cover: ISO updatedAt to epoch ms and numeric relationship ids to strings; overrideAccess false plus user and req on listTasks, loadRecord and the compare-and-set updates; overrideAccess true on activity, notification, due-item and email writes and reads; compare-and-set miss returning undefined; per-document update errors rethrown; workflow mapping with stages sorted by position; notification duplicates found by lookup, by Payload ValidationError and by a raw UNIQUE constraint message; due items filtered on terminal stages and on the [from, to) bounds; a folded Subject; the missing Message-ID SHA-256 fallback; a repeated Message-ID; the 200 KB body cut."
    },
    { "command": "pnpm typecheck", "exitCode": 0, "evidence": "pnpm -r --parallel exec tsc --noEmit printed no diagnostics." },
    {
      "command": "pnpm lint",
      "exitCode": 0,
      "evidence": "eslint . --max-warnings 0 printed no problems. The first run on the new files reported 9 errors, all fixed: toStage complexity 9, describe callbacks over 40 lines, unbound-method, an unnecessary assertion and prefer-specific-assertions."
    },
    {
      "command": "pnpm format:check",
      "exitCode": 0,
      "evidence": "All matched files use Prettier code style! (after prettier --write on both directories)"
    },
    {
      "command": "pnpm check:brand",
      "exitCode": 1,
      "evidence": "apps/web/wrangler.jsonc:108 and :165: check:brand: 2 finding(s). Both are in a committed file outside this scope that the WP did not change; no finding in packages/adapters/payload."
    },
    {
      "command": "pnpm depcruise",
      "exitCode": 0,
      "evidence": "no dependency violations found (214 modules, 385 dependencies cruised)"
    },
    {
      "command": "pnpm jscpd",
      "exitCode": 0,
      "evidence": "Found 13 clones; typescript 0.81% duplicated tokens overall; none in repositories/. The first run flagged the SHA-256 hex helper as a clone of packages/adapters/cloudflare/src/mail/logging-inbound-sink.ts; it was rewritten because adapters cannot import each other."
    },
    {
      "command": "pnpm knip",
      "exitCode": 1,
      "evidence": "Remaining findings are outside this scope and existed before: unused files apps/web/src/server/work/{deps,memory-task-repository,task-repository}.ts; unused dependencies in apps/web and packages/ui. The first run flagged the exported Method type in test/repositories/fake-payload.ts, which is now unexported."
    },
    {
      "command": "pnpm harness:record M1-W8 --attempt 1 --base 8277345",
      "exitCode": 0,
      "evidence": "PASS pnpm typecheck, pnpm lint, format:check, typecheck, lint, vitest run --changed, check:scope; suggestedClasses: none; wrote harness/metrics/attempts/M1-W8-a1.json (filesChanged 16, scopeViolations [])."
    }
  ],
  "testsAdded": [
    "packages/adapters/payload/test/repositories/task-repository.test.ts",
    "packages/adapters/payload/test/repositories/notification-store.test.ts",
    "packages/adapters/payload/test/repositories/due-item-source.test.ts",
    "packages/adapters/payload/test/repositories/email-message-sink.test.ts"
  ],
  "choices": [
    "Exports for the lead to add to packages/adapters/payload/src/index.ts: export { createDueItemSource, createEmailMessageSink, createNotificationStore, createTaskRepository } from './repositories'. Signatures: createTaskRepository(req: PayloadRequest): TaskRepository; createNotificationStore(payload: Payload): NotificationStore; createDueItemSource(payload: Payload), which needs no other argument; createEmailMessageSink(payload: Payload, clock: Clock = systemClock). The due-item and sink port types are declared locally and not exported, because adapters may not import each other.",
    "Payload 3.89 update by where (read in dist/collections/operations/update.js and utilities/update.js): with overrideAccess false, access.update runs first. A false result throws Forbidden; a Where result is ANDed into the query. Matching documents are then read with db.find (pagination false), and each is written by id through updateDocument and db.updateOne. Per-document failures (validation, hooks, database) are caught and returned as result.errors [{ id, isPublic, message }], not thrown. No match returns { docs: [], errors: [] }. updateDocument always overwrites data.updatedAt with new Date().toISOString().",
    "Also verified in the installed sources: the drizzle adapter turns date where values into new Date(v).toISOString(), so equality on an ISO updatedAt with millisecond precision works. createLocalReq sets req.user = options.user || req.user. find with pagination false still applies limit, and limit 0 reads everything. handleUpsertError converts only errors whose code is SQLITE_CONSTRAINT_UNIQUE or 23505 into a ValidationError whose path is the field or column name; it rethrows every other error unchanged.",
    "Compare-and-set (updateIfUnchanged): update by where { and: [{ id equals }, { updatedAt equals <expected ISO> }] } with overrideAccess false, user and req. It returns docs[0], or undefined when nothing matched. A non-empty errors array is rethrown as Error('<collection> update failed: <message>'). An expectedUpdatedAt that is not a valid date returns undefined without a query.",
    "Documents are typed as object and fields are read with Reflect.get, following the coordinator's note that generated payload types now exist.",
    "listTasks reads every task visible to the user, sorted ['rank', 'id'] (D-29), with no pagination. Tasks without workflow, stageId or updatedAt are skipped; loadRecord and saveStage return undefined for them. A missing stageEnteredAt falls back to createdAt, then updatedAt. An unknown priority maps to none.",
    "loadRecord and saveStage support task and project; any other record type returns undefined without a query. Task records carry assigneeIds and groupId. Project records carry ownerId, and project members become assigneeIds, so that `can` agrees with the project staff scope, which includes members.",
    "loadWorkflow(id) and loadTaskWorkflow() read with the user's access. loadTaskWorkflow takes the earliest created workflow with recordType task (sort createdAt, limit 1) and throws Error('No task workflow is configured') when none exists. Stages missing an id, name or known category are dropped; an unknown color maps to gray; probability is kept only when set; stages are sorted by position.",
    "addActivity creates { recordType, recordId, verb, actor, data, occurredAt } in activity with overrideAccess true on the same req. addTransition is a no-op resolving undefined, because the spike has no stageTransitions collection.",
    "The notification store and the email sink share insertUnique. It looks the value up (limit 1, overrideAccess true), then creates. A create error counts as a duplicate when it is a ValidationError whose path matches the field (ignoring underscores and case) with a message matching /unique/i, or when an error or its causes (up to 4 levels) contain 'UNIQUE constraint failed' and the column name. Every other error is rethrown.",
    "Due items: terminal stage ids are collected from every task workflow (system read). The task query is { and: [dueAt >= from, dueAt < to, { or: [stageId not_in terminal, stageId exists false] }] }, sort dueAt, the given limit, overrideAccess true. The stage clause is omitted when no terminal stage exists, and a task without a stage counts as open. Results are checked again in memory against the window and the terminal set. A limit <= 0 returns [] without reading, because Payload treats 0 as no limit.",
    "Email sink: textBody is TextDecoder over the first 200,000 bytes; a multibyte character cut at the end becomes U+FFFD. Headers are parsed from that text: continuation lines are unfolded, names are lower-cased and the first value wins. Subject is cut to 998 characters. A missing, empty or longer than 998 characters Message-ID is replaced by 'sha256:' plus the hex digest of the full raw bytes. to is [envelopeTo]; occurredAt comes from the clock. RFC 2047 encoded words are stored undecoded. Storage errors return err INTERNAL 'inbound message could not be stored' without detail, because the port returns Result and the error could quote the envelope. Nothing is logged."
  ],
  "openQuestions": [
    "The compare-and-set is not atomic on D1. Payload's update by where reads the matching ids, then writes each by id in a separate statement, so a concurrent write landing between the two is overwritten. Two updates in the same millisecond also share an updatedAt. For D-36, M1-L3 can accept this window or add a conditional UPDATE ... WHERE updated_at = ? through payload.db, which bypasses hooks and access.",
    "Not verified against a real D1 binding: whether a D1 unique violation reaches Payload with code SQLITE_CONSTRAINT_UNIQUE (and becomes a ValidationError) or only as message text. The detector handles both shapes; M1-W6's duplicate cron run on local D1 would confirm it.",
    "Stage transitions are not persisted until a stageTransitions collection exists; changeStage currently records only the stage.changed activity.",
    "Please confirm that project members should count as assignees for `can` on project stage changes.",
    "listTasks is unbounded; D-34 pagination is not applied in the spike.",
    "The adapter README Public API section is outside this write scope. It needs the four factories added when the barrel export lands.",
    "check:brand fails on apps/web/wrangler.jsonc:108 and :165, and knip reports apps/web and packages/ui findings. All of these predate this WP and are outside its scope."
  ]
}
---

# Report: M1-W8

The four Payload Local API repositories are in `packages/adapters/payload/src/repositories/` and are exported from `repositories/index.ts`. Their 21 tests use a fake `payload` that records every call. All gates pass for this scope. Outside the scope, `check:brand` (`apps/web/wrangler.jsonc`) and `knip` (`apps/web`, `packages/ui`) still fail on findings that were already there.

Look at these first:

1. The compare-and-set is not atomic on D1: Payload's update by `where` reads the matching ids, then writes each one by id, so a write landing in between is overwritten (open question 1, D-36).
2. `addTransition` is a no-op until a `stageTransitions` collection exists.
3. The lead adds `export { createDueItemSource, createEmailMessageSink, createNotificationStore, createTaskRepository } from './repositories'` to the package barrel.
