---
{
  "wp": "M1-W7",
  "attempt": 1,
  "status": "DONE",
  "filesChanged": [
    "scripts/spike/startup.ts",
    "scripts/spike/latency.ts",
    "scripts/spike/analytics.ts",
    "scripts/spike/lib/cli.ts",
    "scripts/spike/lib/output.ts",
    "scripts/spike/lib/stats.ts",
    "scripts/spike/lib/process.ts",
    "scripts/spike/lib/http.ts",
    "scripts/spike/lib/graphql.ts",
    "scripts/spike/lib/analytics-parse.ts",
    "scripts/test/spike/stats.test.ts",
    "scripts/test/spike/common.test.ts",
    "scripts/test/spike/startup.test.ts",
    "scripts/test/spike/latency.test.ts",
    "scripts/test/spike/analytics.test.ts",
    "scripts/test/spike/capture.ts",
    "scripts/test/spike/fixtures/wrangler-versions-upload.txt",
    "scripts/test/spike/fixtures/workers-invocations-docs.json",
    "docs/spike-data/README.md",
    "docs/orchestration/m1/reports/M1-W7.md",
    "harness/metrics/attempts/M1-W7-a1.json"
  ],
  "acceptance": [
    {
      "command": "pnpm exec vitest run scripts/test/spike",
      "exitCode": 0,
      "evidence": "Test Files  5 passed (5); Tests  43 passed (43)"
    },
    {
      "command": "pnpm typecheck",
      "exitCode": 0,
      "evidence": "pnpm -r --parallel exec tsc --noEmit printed no diagnostics. pnpm -r does not cover the root scripts/ project, so pnpm exec tsc -p scripts/tsconfig.json --noEmit was also run: exit 2 with 80 errors, all in scripts/harness and scripts/test/harness (pre-existing); none in scripts/spike or scripts/test/spike."
    },
    {
      "command": "pnpm lint",
      "exitCode": 0,
      "evidence": "eslint . --max-warnings 0 printed no findings (covers scripts/spike and scripts/test/spike through scripts/tsconfig.json). No eslint-disable comments were added."
    },
    {
      "command": "pnpm format:check",
      "exitCode": 0,
      "evidence": "All matched files use Prettier code style!"
    },
    {
      "command": "pnpm jscpd",
      "exitCode": 0,
      "evidence": "Found 13 clones (typescript 0 duplicated lines, yaml 232); `pnpm jscpd | grep -i spike` printed nothing."
    },
    {
      "command": "pnpm knip",
      "exitCode": 1,
      "evidence": "No findings in scope. All findings are outside it: unused files apps/web/src/server/work/{deps,memory-task-repository,task-repository}.ts; unused dependencies @ops/kernel, @ops/platform (apps/web), @atlaskit/pragmatic-drag-and-drop, @atlaskit/pragmatic-drag-and-drop-hitbox, @svar-ui/react-gantt (packages/ui); configuration hint src/migrations/**."
    },
    {
      "command": "pnpm check:brand",
      "exitCode": 1,
      "evidence": "apps/web/wrangler.jsonc:108 mirch / apps/web/wrangler.jsonc:165 mirch / check:brand: 2 finding(s). This WP did not touch that file. No brand tokens appear in scripts/spike, and the tenant hostname is passed only as an argument."
    },
    {
      "command": "pnpm exec tsx scripts/spike/startup.ts --env staging-a --count 2 --deploy --cold-url https://ops-staging-a.example.workers.dev/login --dry-run",
      "exitCode": 0,
      "evidence": "[dry-run] run 1: pnpm --filter web exec wrangler versions upload --env staging-a / [dry-run] run 1: pnpm --filter web exec wrangler versions deploy '<version-id-of-run-1>@100%' --yes --message 'startup measurement run 1' --env staging-a / [dry-run] run 1: wait 15000 ms, then GET https://ops-staging-a.example.workers.dev/login once (cold request, fetch) / (same for run 2) / [dry-run] write docs/spike-data/startup-staging-a-20260913T134732Z.json"
    },
    {
      "command": "pnpm exec tsx scripts/spike/latency.ts --env staging-a --base-url https://ops-staging-a.example.workers.dev --path /login --path /api/v1/health --cold --header 'Cookie: payload-token=abc123' --dry-run",
      "exitCode": 0,
      "evidence": "[dry-run] cold, 1 request: curl -sS -o /dev/null -w '%{http_code} %{time_total}' -X GET -H 'Cookie: [redacted]' https://ops-staging-a.example.workers.dev/login / [dry-run] warm, 200 sequential requests: curl ... -H 'Cookie: [redacted]' https://ops-staging-a.example.workers.dev/login / [dry-run] warm, 200 sequential requests: curl ... /api/v1/health / [dry-run] write docs/spike-data/latency-staging-a-20260913T134734Z.json. With --client fetch --method POST --header 'Next-Action: 7f3a' --data '[]': [dry-run] warm, 200 sequential requests: POST https://ops-staging-a.example.workers.dev/tasks / Next-Action: 7f3a / body: []"
    },
    {
      "command": "CLOUDFLARE_API_TOKEN=fake-token-for-redaction-check pnpm exec tsx scripts/spike/analytics.ts --env staging-a --start 2026-09-13T14:00:00Z --end 2026-09-13T15:00:00Z --script ops-staging-a --database-id 00000000-0000-4000-8000-000000000000 --bucket ops-staging-a --dry-run",
      "exitCode": 0,
      "evidence": "Three blocks: [dry-run] POST https://api.cloudflare.com/client/v4/graphql / Authorization: Bearer [redacted] / body: {\"query\":\"query SpikeWorkers(...) { ... workersInvocationsAdaptive(limit: 10000, filter: { scriptName: $scriptName, datetime_geq: $start, datetime_leq: $end }) { sum { requests errors subrequests } quantiles { cpuTimeP50 cpuTimeP99 } dimensions { scriptName } } ... }\",\"variables\":{\"accountTag\":\"<CLOUDFLARE_ACCOUNT_ID>\",...}}, then SpikeD1 (d1AnalyticsAdaptiveGroups) and SpikeR2 (r2OperationsAdaptiveGroups); [dry-run] write docs/spike-data/analytics-staging-a-20260913T134738Z.json. grep -c for the token value in the output: 0."
    },
    {
      "command": "pnpm exec tsx scripts/spike/latency.ts --env local --base-url http://localhost:3000 --path /tasks --count 20 --cold --out-dir <scratchpad>/spike-data",
      "exitCode": 0,
      "evidence": "Against pnpm dev (Next.js 16.3.5): latency: cold /tasks 1873.339 ms (status 200) / latency: /tasks p50 65.726 ms, p95 77.049 ms (20 requests) / wrote latency-local-20260913T134758Z.json (client curl). With --client fetch: latency: /tasks p50 67.371 ms, p95 69.558 ms (20 requests). Output not committed. Afterwards the dev server was stopped, apps/web/next-env.d.ts (modified by next dev) restored and apps/web/.next removed."
    },
    {
      "command": "pnpm harness:record M1-W7 --attempt 1 --base b45d4b5",
      "exitCode": 0,
      "evidence": "PASS format:check; PASS typecheck; PASS lint; PASS vitest run --changed; PASS check:scope; suggestedClasses: none; wrote harness/metrics/attempts/M1-W7-a1.json"
    }
  ],
  "testsAdded": [
    "scripts/test/spike/stats.test.ts (7 tests: single sample for every p, even counts take the lower middle as p50, odd counts, whole ranks without float drift (p95 of 20 and 200 samples), p0/p100, empty set and p outside [0, 100] rejected, input not reordered, summarize)",
    "scripts/test/spike/common.test.ts (7 tests: ISO basic timestamp and file name, millisecond rounding, secret redaction, --env validation, integer flags with minimum and fallback, URL/instant/required flags, shell quoting of formatted commands)",
    "scripts/test/spike/startup.test.ts (6 tests: startup time and version id parsed from the wrangler versions upload sample, null for missing or 'undefined' values, defaults and wrangler command, invalid arguments, dry-run prints upload/deploy/cold/write and writes nothing, min/median/max record)",
    "scripts/test/spike/latency.test.ts (9 tests: defaults, invalid arguments, curl write-out parsing, curl arguments, credential header redaction, fetch run against a local node:http server writing cold plus p50/p95 JSON, loud failure on 500 with nothing written, dry-run with redacted cookie, empty sample set)",
    "scripts/test/spike/analytics.test.ts (14 tests: Workers, D1 and R2 query bodies and variables, authenticated POST request, docs Workers sample response parsed, single-group quantiles, D1 hour sums, R2 counts by action type, GraphQL errors and bad shape, option validation, missing credentials named, token redaction, dry-run prints three POSTs without reading env credentials)"
  ],
  "choices": [
    "Startup wording comes from the installed wrangler 4.131.1 (apps/web/node_modules/wrangler/wrangler-dist/cli.js): versions upload calls logger.log(\"Worker Startup Time:\", result.startup_time_ms, \"ms\") unconditionally (so 'undefined ms' is possible and parses as null), then logger.log(\"Worker Version ID:\", versionId). Parsed with /Worker Startup Time:\\s*(\\d+(?:\\.\\d+)?)\\s*ms/. The fixture is assembled from those source strings (Total Upload, bindings table, Uploaded, Version ID, Version Preview URL, deploy hint); no real upload output was recorded, since that needs credentials.",
    "startup.ts runs `pnpm --filter web exec wrangler versions upload --env <env>` N times (default 5) without a shell and fails on a non-zero exit or a missing startup line. With --deploy, each uploaded version is deployed with `wrangler versions deploy <id>@100% --yes --message 'startup measurement run <n>' --env <env>` (flags checked in the same wrangler source). With --cold-url, it then waits --settle-ms (default 15000) and times one GET with fetch. This covers 'cold first-request latency after each of those 5 deploys' in one command. The record has startupTimeMs and coldMs as {count, min, median, max} plus per-run values and version ids.",
    "latency.ts defaults to --client curl (spec wording): `curl -sS -o /dev/null -w '%{http_code} %{time_total}' -X <method> [-H ...] [--data-binary ...] <url>`, one process and connection per request, redirects not followed. --client fetch times from send to fully read body with performance.now(), redirect: 'manual', keep-alive connections. The client and a description of its timer are recorded as metadata. --cold times one request to the first --path before the warm loop. Any status outside 2xx/3xx throws immediately and no file is written. Header values are never written, only headerNames; Authorization, Cookie and Proxy-Authorization values print as [redacted] in dry-run. Samples are stored as samplesMs (3 decimals) plus statusCounts per path.",
    "Percentiles: nearest rank, the value at rank ceil(p * n / 100), at least 1, computed with integer arithmetic before dividing to avoid float drift. Median is p50, so an even count takes the lower middle value. The method string is written into every output file.",
    "Output naming: <out-dir>/<metric>-<env>-<timestamp>.json with the UTC start time in ISO 8601 basic format (20260913T142233Z), because colons are awkward in file names. The default out-dir is docs/spike-data relative to the cwd. JSON is two-space indented with a trailing newline.",
    "Dry-run is a shared flag: each script prints '[dry-run] <command | request>' for every action plus '[dry-run] write <file>', and returns before any process, network or file write. analytics.ts does not read CLOUDFLARE_API_TOKEN or CLOUDFLARE_ACCOUNT_ID in dry-run; it prints <CLOUDFLARE_ACCOUNT_ID> as the accountTag and 'Authorization: Bearer [redacted]'. In a real run the token is used only in the Authorization header, and error bodies pass through redact() before printing.",
    "GraphQL, checked against the docs listed in scripts/spike/lib/graphql.ts (fetched as index.md on 2026-09-13): endpoint https://api.cloudflare.com/client/v4/graphql, POST with a single-line query and variables (querying-basics). Workers: workersInvocationsAdaptive(limit, filter { scriptName, datetime_geq, datetime_leq }) { sum { requests errors subrequests } quantiles { cpuTimeP50 cpuTimeP99 } dimensions { scriptName } }, with variable types string as in the tutorial. D1: d1AnalyticsAdaptiveGroups sum { readQueries writeQueries rowsRead rowsWritten } (field names from the D1 metrics table), filtered on databaseId plus datetimeHour_geq/leq as in wrangler 4.131.1's D1 info query. The D1 docs example uses date_geq with Date; hour granularity was chosen as the finer option. --start is floored to the hour. R2: r2OperationsAdaptiveGroups(filter { bucketName, datetime_geq, datetime_leq }) { sum { requests } dimensions { actionType } } with Time variables, as in the R2 docs. The token needs Account > Account Analytics > Read (api-token-auth page).",
    "Workers quantiles cannot be merged across groups: parseWorkers returns top-level cpuTimeP50/cpuTimeP99 only when the response has exactly one group (expected, since the only dimension is the filtered scriptName) and always returns quantilesByGroup. The docs sample, with 3 groups, parses to requests 6 and null top-level quantiles. The analytics record includes window, targets, endpoint and docs URLs, never the account id.",
    "Doc URLs relied on: https://developers.cloudflare.com/analytics/graphql-api/tutorials/querying-workers-metrics/ ; https://developers.cloudflare.com/d1/observability/metrics-analytics/ ; https://developers.cloudflare.com/r2/platform/metrics-analytics/ ; https://developers.cloudflare.com/analytics/graphql-api/getting-started/querying-basics/ ; https://developers.cloudflare.com/analytics/graphql-api/getting-started/authentication/api-token-auth/ ; https://developers.cloudflare.com/analytics/graphql-api/features/data-sets/ (workersInvocationsAdaptive is an aggregated node despite lacking the Groups suffix) ; https://developers.cloudflare.com/workers/platform/limits/ (startup time limit 1 second; wrangler versions upload reports startup_time_ms).",
    "Lead commands, from the repository root after M1-L3's build and deploy, with wrangler logged in: (1) pnpm exec tsx scripts/spike/startup.ts --env staging-a --count 5 --deploy --cold-url https://ops-staging-a.mirchmedia01.workers.dev/login ; (2) pnpm exec tsx scripts/spike/latency.ts --env staging-a --base-url https://ops-staging-a.mirchmedia01.workers.dev --path /login --path /api/v1/health ; pnpm exec tsx scripts/spike/latency.ts --env staging-a --base-url https://ops-staging-a.mirchmedia01.workers.dev --path /tasks --header 'Cookie: <session cookie>' ; pnpm exec tsx scripts/spike/latency.ts --env staging-a --base-url https://ops-staging-a.mirchmedia01.workers.dev --path /tasks --method POST --header 'Cookie: <session cookie>' --header 'Next-Action: <action id>' --data '<action body>' ; (3) at least 5 minutes later, with CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID exported: pnpm exec tsx scripts/spike/analytics.ts --env staging-a --start <ISO instant just before step 1> --script ops-staging-a --database-id <database_id of ops-staging-a from apps/web/wrangler.jsonc env.staging-a> --bucket ops-staging-a ; (4) pnpm size ; then pnpm exec prettier --write docs/spike-data before committing. Append --dry-run to any command to preview it.",
    "Shared code lives in scripts/spike/lib (cli, output, stats, process, http, graphql, analytics-parse), reusing isMain from scripts/lib/report.ts. No package.json scripts, dependencies or tooling were changed. Only Node built-ins are used (node:util parseArgs, fetch, child_process spawnSync, perf_hooks, timers/promises)."
  ],
  "openQuestions": [
    "The CPU time unit is not stated on the docs pages used. The docs sample values (206, 291, 212.5) suggest microseconds. The scripts keep the raw field names cpuTimeP50/cpuTimeP99; confirm the unit with GraphQL introspection before comparing to milliseconds in the spike report.",
    "The D1 filter on datetimeHour_geq/leq and the Time variable types, Workers' string datetime variables and R2's Time variables follow the docs and wrangler source, but have never been sent to the live API (no token). If the API rejects a variable type, change only the query header in scripts/spike/lib/graphql.ts. D1 counts cover whole UTC hours, so other traffic in those hours is included.",
    "startup.ts --deploy creates 5 deployments of staging-a at 100%, and the cold request goes out after a fixed 15 s settle with no check of which version served it. If the Worker exposes version metadata (for example a CF_VERSION_METADATA binding in a header), the lead can confirm the version; otherwise raise --settle-ms.",
    "docs/spike-data is not in .prettierignore, so committed JSON must be run through prettier --write (documented in the README). Alternatively the lead adds docs/spike-data/*.json to .prettierignore (lead-owned).",
    "/tasks and the board action need an authenticated session after M1-L3, and the board action needs its Next-Action id and body. The latency script takes them as --header/--data; the lead supplies the values. Cookie values are redacted in dry-run and never written to output.",
    "pnpm typecheck (pnpm -r) does not include the root scripts/ project; tsc -p scripts/tsconfig.json reports 80 pre-existing errors in scripts/harness and scripts/test/harness. pnpm lint does type-aware checks on scripts/spike through the project service."
  ]
}
---

# Report: M1-W7

Measurement scripts for the M1 spike are in `scripts/spike/`: `startup.ts`, `latency.ts` and `analytics.ts`, with a shared library in `scripts/spike/lib/`. There are 43 Vitest tests in `scripts/test/spike/`. `docs/spike-data/README.md` gives the run order. Nothing contacted Cloudflare: startup and analytics were exercised with `--dry-run` and tests only. Latency ran against `pnpm dev` (`/tasks`, 20 requests, curl: p50 65.726 ms, p95 77.049 ms).

Look at these first:

1. `startup.ts --deploy --cold-url` deploys each of the 5 uploaded versions and times one cold request after a 15 s settle.
2. The D1 GraphQL filter uses `datetimeHour` (from wrangler's source, not the D1 docs example), so D1 counts cover whole hours.
3. The CPU time quantile unit is not confirmed by the docs used.
4. Committed spike JSON needs `prettier --write` before `format:check` passes.
