---
{
  "wp": "M1-W4",
  "attempt": 1,
  "status": "DONE",
  "filesChanged": [
    "packages/ui/src/composites/KanbanBoard/KanbanBoard.tsx",
    "packages/ui/src/composites/KanbanBoard/KanbanCardItem.tsx",
    "packages/ui/src/composites/KanbanBoard/KanbanColumn.tsx",
    "packages/ui/src/composites/KanbanBoard/board-state.ts",
    "packages/ui/src/composites/KanbanBoard/index.ts",
    "packages/ui/src/composites/KanbanBoard/stage-dot.ts",
    "packages/ui/src/composites/KanbanBoard/types.ts",
    "packages/ui/test/composites/KanbanBoard/board-state.test.ts",
    "apps/web/src/app/(app)/tasks/board/page.tsx",
    "apps/web/src/app/(app)/tasks/board/TaskBoard.tsx",
    "apps/web/src/app/(app)/tasks/board/loading.tsx",
    "apps/web/src/server/actions/work/tasks/moveTask.ts",
    "apps/web/test/actions/work/tasks/moveTask.test.ts",
    "harness/metrics/attempts/M1-W4-a1.json",
    "docs/orchestration/m1/reports/M1-W4.md"
  ],
  "acceptance": [
    {
      "command": "pnpm exec vitest run packages/ui apps/web",
      "exitCode": 0,
      "evidence": "Test Files  2 passed (2); Tests  19 passed (19)"
    },
    {
      "command": "pnpm typecheck",
      "exitCode": 0,
      "evidence": "No diagnostics. The first run reported TS4111 in board-state.ts (index-signature access under noPropertyAccessFromIndexSignature); fixed with bracket access."
    },
    { "command": "pnpm lint", "exitCode": 0, "evidence": "eslint . --max-warnings 0 printed no problems; no disables." },
    { "command": "pnpm format:check", "exitCode": 0, "evidence": "All matched files use Prettier code style!" },
    {
      "command": "pnpm check:brand",
      "exitCode": 1,
      "evidence": "apps/web/wrangler.jsonc:108 mirch / apps/web/wrangler.jsonc:165 mirch / check:brand: 2 finding(s). Both are in a lead-owned critical path, unchanged from b45d4b5; none in this WP's files."
    },
    {
      "command": "pnpm depcruise",
      "exitCode": 0,
      "evidence": "✔ no dependency violations found (189 modules, 312 dependencies cruised)"
    },
    {
      "command": "pnpm jscpd",
      "exitCode": 0,
      "evidence": "Total 1.25% duplicated lines (threshold 2%). One tsx clone: apps/web/src/app/(app)/tasks/board/page.tsx [38:1 - 50:15] vs tasks/page.tsx [49:1 - 61:15] (13 lines, 64 tokens), the sidebar NAV array."
    },
    {
      "command": "pnpm knip",
      "exitCode": 1,
      "evidence": "Unused dependencies (1): @svar-ui/react-gantt packages/ui/package.json (M1-W5). Unused exported types (1): TaskDatesInput apps/web/src/server/work/task-repository.ts. Configuration hints (1): src/migrations/**. None in this WP's scope; the pragmatic-drag-and-drop and hitbox dependencies and the work/ port files are no longer reported."
    },
    {
      "command": "pnpm --filter web build",
      "exitCode": 0,
      "evidence": "⚠ Compiled with warnings in 24.5s (no warning text printed); Route (app): ├ ƒ /tasks, └ ƒ /tasks/board"
    },
    {
      "command": "pnpm dev (background) && curl -s -o board.html -w 'HTTP %{http_code}' http://localhost:3000/tasks/board",
      "exitCode": 0,
      "evidence": "HTTP 200; body contains Backlog, To do, In progress, Review, Done and 'Draft onboarding checklist'; dev log: GET /tasks/board 200 in 1876ms."
    },
    {
      "command": "node <scratchpad>/board-move.mjs (Playwright 1.63, against pnpm dev; not committed)",
      "exitCode": 0,
      "evidence": "drag: before stage-todo / drag: action status 200 / drag: after drop stage-progress / drag: after reload stage-progress / menu: before stage-todo / menu: card draggable attr null / menu: action status 200 / menu: after reload stage-review. Drag case: 1440x900 viewport, locator.dragTo from task-1 to the In progress column. Menu case: 390x844, hasTouch, isMobile, 'Move to…' then 'Review' on task-6. Needed pnpm exec playwright install chromium (headless shell v1243). Dev server stopped afterwards."
    },
    {
      "command": "pnpm harness:record M1-W4 --attempt 1 --base b45d4b5",
      "exitCode": 0,
      "evidence": "Run on commit ede93e0. PASS format:check, PASS typecheck, PASS lint, PASS vitest run --changed, PASS check:scope; suggestedClasses: none; wrote harness/metrics/attempts/M1-W4-a1.json"
    }
  ],
  "testsAdded": [
    "packages/ui/test/composites/KanbanBoard/board-state.test.ts",
    "apps/web/test/actions/work/tasks/moveTask.test.ts"
  ],
  "choices": [
    "Pure board logic lives in board-state.ts: initialCollapsed/toggleCollapsed, groupCards (sorted by title, unknown stages dropped), planMove, applyMove, confirmMove, rollbackMove (restores only the failed card), settleMove (a thrown or rejected onMove becomes an INTERNAL failure so the card rolls back), readCardData and dropStageId.",
    "The composite is record-agnostic: KanbanCard {id, stageId, title, updatedAt, meta?: ReactNode}, and onMove receives {cardId, toStageId, expectedUpdatedAt}. The client wrapper apps/web/.../board/TaskBoard.tsx maps cardId to taskId for moveTask and passes router.refresh as onConflict, because an RSC page cannot pass an inline function or useRouter.",
    "Optimistic state is useState seeded from props; new server props (after revalidatePath or refresh) replace it. On failure the card rolls back and a sonner toast appears: CONFLICT shows labels.conflict and calls onConflict; other codes show labels.moveFailed with the error message as description.",
    "@ops/ui declares the stage category and color unions locally, because the package has no @ops/kernel or @ops/platform dependency; the page maps platform Stage to KanbanStage sorted by position.",
    "Drag is enabled via useSyncExternalStore on matchMedia('(min-width: 48rem) and (pointer: fine)'), false on the server. The 'Move to…' trigger is hidden with md:pointer-fine:hidden, so the CSS and JS use the same condition without a first-paint flash on narrow screens.",
    "Columns and cards are both pdnd drop targets; the drop resolves to the innermost target carrying a stageId. Card targets use hitbox attachClosestEdge (top/bottom) for a drop indicator and only accept cards from other columns. After the drop, the card is still placed by title order.",
    "Terminal columns render as a w-12 (48 px) rail: expand toggle, dot, count, then the name with writing-mode vertical-rl. Expanded terminal columns show a collapse toggle; both toggles carry aria-expanded and a '{name}' label template. Collapsed rails are still drop targets.",
    "moveTask.ts exports only async functions: runMoveTask(deps, input: unknown) validates by hand and calls changeStage with { actor, can, store: tasks, uow, clock }; moveTask(input) calls getWorkDeps and revalidates /tasks/board and /tasks on success. Error details are not returned to the client.",
    "The board page sets dynamic = 'force-dynamic' and reads the sidebar cookie before getWorkDeps, so a production build never calls the development-only deps. <Toaster /> is mounted in the page because the layout is lead-owned.",
    "loading.tsx renders four skeleton columns. The existing tasks/error.tsx boundary also covers /tasks/board."
  ],
  "openQuestions": [
    "jscpd reports the NAV array shared with tasks/page.tsx (13 lines, 64 tokens). It goes away when M1-L3 moves AppShell and the sidebar into (app)/layout.tsx; <Toaster /> should move there at the same time.",
    "Card drop targets show a top/bottom edge indicator, but the drop does not persist order (cards stay sorted by title until rank writes arrive, D-29). If the indicator reads as misleading in the spike, remove the card targets; the hitbox dependency would then become unused again.",
    "runMoveTask is exported from a 'use server' file for the test, so Next may register it as an action. A client cannot pass the function-valued deps, so a call fails before any write; moving the core into a non-action module would need a path outside this WP's scope.",
    "pnpm dev rewrote apps/web/next-env.d.ts again; it was restored and is not committed.",
    "check:brand fails on apps/web/wrangler.jsonc lines 108 and 165 at the base commit; the generated Worker config needs a brand-free value or an exclusion.",
    "M1-L3: export KanbanBoard from packages/ui/src/index.ts and list it in packages/ui/README.md; add a Table | Board ViewSwitcher (spec §17.5) linking /tasks and /tasks/board."
  ]
}
---

# Report: M1-W4

The spike board is in place on `wp/M1-W4` (code commit ede93e0):

- The `KanbanBoard` composite under `packages/ui/src/composites/KanbanBoard/`, with the pure move and column logic in `board-state.ts`.
- The `/tasks/board` route: an RSC page, a client wrapper and a loading skeleton.
- The `moveTask` server action, which calls platform `changeStage` with `expectedUpdatedAt` through `getWorkDeps()`.

Acceptance results:

- The unit tests (19), typecheck, lint, format, depcruise and jscpd pass, and the web build lists `ƒ /tasks/board`.
- The action tests show that a stale `expectedUpdatedAt` returns CONFLICT, and that both an unknown stage and invalid input return VALIDATION.
- On `pnpm dev`, a Playwright drag moved a task from To do to In progress. At phone width, the "Move to…" menu moved another task to Review. Both moves survived a reload.
- `pnpm harness:record M1-W4 --attempt 1` passed every gate.
- `check:brand` and `knip` still fail, but only on findings outside this WP's files.

Look at these first:

1. The composite takes generic card moves, and `TaskBoard.tsx` adapts them to `moveTask` and supplies `router.refresh`.
2. The card drop targets use hitbox for an edge indicator, but the order is not persisted.
3. The sidebar nav duplicates the tasks page until the shell moves into the layout.
