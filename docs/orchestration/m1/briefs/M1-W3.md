# Brief: M1-W3 — Spike background work

- **Milestone:** M1 · **Attempt:** 1 · **Branch:** `wp/M1-W3` from `m1-spike`
- **Objective:** Spike background work: `CloudflareMailSender` and `ConsoleMailSender` implementing platform `MailSender`, internal cron and inbound routes guarded by `INTERNAL_SECRET_HEADER` at `INTERNAL_ROUTES`, one due-soon reminder job writing through platform `NotificationStore` with a dedupe key
- **Spec references:** §21.3 (M1 work packages)
- **Write scope:** `packages/adapters/cloudflare/src/mail/`; `packages/adapters/cloudflare/src/cron/`; `packages/adapters/cloudflare/test/mail/`; `packages/adapters/cloudflare/test/cron/`; `apps/web/src/app/api/v1/internal/`
- **Inputs:** merged work packages M1-L1; row in `docs/orchestration/m1/plan.md`
- **Acceptance:** unit tests pass; running the job twice against an in-memory `NotificationStore` creates one notification; `pnpm harness:record M1-W3 --attempt 1`
- **Boundaries:** do not change spec §0.7 critical paths; no dependency, lockfile or migration changes; follow spec §3, §5.2, §6; decide ordinary implementation details yourself and list them under choices
- **Report:** `docs/orchestration/m1/reports/M1-W3.md` with front matter per `harness/schemas/report.schema.json`

## Known pitfalls
None.

## Previous attempt findings
None (first attempt).
