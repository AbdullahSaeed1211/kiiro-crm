# Brief: M1-W9 — Local development data and health

- **Milestone:** M1 · **Attempt:** 1 · **Branch:** `wp/M1-W9` from `m1-spike`
- **Objective:** Local development data and health: `scripts/seed-dev.ts` (spec §6.9 subset: settings, owner, manager and two staff users with groups and reports, the agency task and project workflows, one organization, two projects, twelve tasks due from −3 to +10 days; idempotent by email or name), `scripts/reset-local-db.ts`, and `GET /api/v1/health` returning `{ status, version, migration }`
- **Spec references:** §6.9, §21.3 (M1 work packages)
- **Write scope:** `scripts/seed-dev.ts`; `scripts/reset-local-db.ts`; `scripts/seed/`; `scripts/test/seed/`; `apps/web/src/app/api/v1/health/`
- **Inputs:** merged work packages M1-W1; row in `docs/orchestration/m1/plan.md`
- **Acceptance:** `pnpm db:reset:local` then `pnpm seed:dev` twice leaves the same row counts; `curl /api/v1/health` returns 200 with the latest migration name on `pnpm dev`; `pnpm harness:record M1-W9 --attempt 1`
- **Boundaries:** do not change spec §0.7 critical paths; no dependency, lockfile or migration changes; follow spec §3, §5.2, §6; decide ordinary implementation details yourself and list them under choices
- **Report:** `docs/orchestration/m1/reports/M1-W9.md` with front matter per `harness/schemas/report.schema.json`

## Known pitfalls
None.

## Previous attempt findings
None (first attempt).
