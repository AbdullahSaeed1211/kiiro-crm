# Brief: M1-W5 — Spike timeline

- **Milestone:** M1 · **Attempt:** 1 · **Branch:** `wp/M1-W5` from `m1-spike`
- **Objective:** Spike timeline: GanttView wrapper (SVAR MIT edition), `/timeline` page, `setTaskDates` server action through `getWorkDeps`
- **Spec references:** §21.3 (M1 work packages)
- **Write scope:** `packages/ui/src/composites/GanttView/`; `packages/ui/test/composites/GanttView/`; `apps/web/src/app/(app)/timeline/`; `apps/web/src/server/actions/work/tasks/setTaskDates.ts`; `apps/web/test/actions/work/tasks/setTaskDates.test.ts`
- **Inputs:** merged work packages M1-L2, M1-W2; row in `docs/orchestration/m1/plan.md`
- **Acceptance:** `/timeline` renders task bars on `pnpm dev`; dragging or resizing a bar persists `startAt`/`dueAt` through `setTaskDates` and survives a reload; tests cover authorization, a start after the due date (VALIDATION) and a stale `expectedUpdatedAt` (CONFLICT); `pnpm harness:record M1-W5 --attempt 1`
- **Boundaries:** do not change spec §0.7 critical paths; no dependency, lockfile or migration changes; follow spec §3, §5.2, §6; decide ordinary implementation details yourself and list them under choices
- **Report:** `docs/orchestration/m1/reports/M1-W5.md` with front matter per `harness/schemas/report.schema.json`

## Known pitfalls
None.

## Previous attempt findings
None (first attempt).
