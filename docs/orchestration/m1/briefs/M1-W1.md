# Brief: M1-W1 — Spike collections users

- **Milestone:** M1 · **Attempt:** 1 · **Branch:** `wp/M1-W1` from `m1-spike`
- **Objective:** Spike collections users (moving the D-38 auth settings from `apps/web/src/collections/Users.ts`), groups, organizations, projects, tasks, workflows, activity, attachments, notifications, emailMessages, jobRuns and the settings global, wired to the lead's §11.1 access functions
- **Spec references:** §11.1, §21.3 (M1 work packages)
- **Write scope:** `packages/adapters/payload/src/collections/`; `packages/adapters/payload/test/collections/`
- **Inputs:** merged work packages M1-L1, M1-L2; row in `docs/orchestration/m1/plan.md`
- **Acceptance:** a collection test builds a Payload config from the exported collections and settings global and checks slugs, `FIELDS` names and attached access; `pnpm typecheck` and `pnpm lint` pass; `pnpm harness:record M1-W1 --attempt 1`
- **Boundaries:** do not change spec §0.7 critical paths; no dependency, lockfile or migration changes; follow spec §3, §5.2, §6; decide ordinary implementation details yourself and list them under choices
- **Report:** `docs/orchestration/m1/reports/M1-W1.md` with front matter per `harness/schemas/report.schema.json`

## Known pitfalls
None.

## Previous attempt findings
None (first attempt).
