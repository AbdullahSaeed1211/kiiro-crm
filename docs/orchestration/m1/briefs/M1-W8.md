# Brief: M1-W8 — Payload repositories

- **Milestone:** M1 · **Attempt:** 1 · **Branch:** `wp/M1-W8` from `m1-spike`
- **Objective:** Payload repositories: `TaskRepository` from `@ops/module-work` (task and project stage store, dates, task workflow, activity), platform `NotificationStore`, a due-items source and an inbound email sink over `emailMessages`, structurally matching the Cloudflare adapter ports, all on the Payload Local API
- **Spec references:** §21.3 (M1 work packages)
- **Write scope:** `packages/adapters/payload/src/repositories/`; `packages/adapters/payload/test/repositories/`
- **Inputs:** merged work packages M1-W1; row in `docs/orchestration/m1/plan.md`
- **Acceptance:** unit tests with a fake Payload cover compare-and-set conflicts, scoped reads with `overrideAccess: false`, duplicate notification dedupe keys and message ids; `pnpm typecheck` and `pnpm lint` pass; `pnpm harness:record M1-W8 --attempt 1`
- **Boundaries:** do not change spec §0.7 critical paths; no dependency, lockfile or migration changes; follow spec §3, §5.2, §6; decide ordinary implementation details yourself and list them under choices
- **Report:** `docs/orchestration/m1/reports/M1-W8.md` with front matter per `harness/schemas/report.schema.json`

## Known pitfalls
None.

## Previous attempt findings
None (first attempt).
