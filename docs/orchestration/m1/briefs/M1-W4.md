# Brief: M1-W4 — Spike board

- **Milestone:** M1 · **Attempt:** 1 · **Branch:** `wp/M1-W4` from `m1-spike`
- **Objective:** Spike board: KanbanBoard composite (pragmatic drag and drop), `/tasks/board` page, `moveTask` server action calling `changeStage` with `expectedUpdatedAt` through `getWorkDeps`
- **Spec references:** §21.3 (M1 work packages)
- **Write scope:** `packages/ui/src/composites/KanbanBoard/`; `packages/ui/test/composites/KanbanBoard/`; `apps/web/src/app/(app)/tasks/board/`; `apps/web/src/server/actions/work/tasks/moveTask.ts`; `apps/web/test/actions/work/tasks/moveTask.test.ts`
- **Inputs:** merged work packages M1-L2, M1-W2; row in `docs/orchestration/m1/plan.md`
- **Acceptance:** `/tasks/board` renders the task workflow on `pnpm dev`; a drop moves the card through `moveTask` and the move survives a reload; tests show a stale `expectedUpdatedAt` returns CONFLICT; `pnpm harness:record M1-W4 --attempt 1`
- **Boundaries:** do not change spec §0.7 critical paths; no dependency, lockfile or migration changes; follow spec §3, §5.2, §6; decide ordinary implementation details yourself and list them under choices
- **Report:** `docs/orchestration/m1/reports/M1-W4.md` with front matter per `harness/schemas/report.schema.json`

## Known pitfalls
None.

## Previous attempt findings
None (first attempt).
