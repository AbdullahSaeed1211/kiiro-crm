---
{
  "wp": "M2-W5",
  "attempt": 3,
  "status": "DONE",
  "filesChanged": [
    "apps/web/src/app/(app)/leads/",
    "apps/web/src/server/crm/leads/",
    "apps/web/src/app/(app)/leads/lead-board-model.ts",
    "apps/web/test/queries/crm/leads/types.test.ts",
    "apps/web/test/queries/crm/leads/board.test.ts",
    "harness/metrics/attempts/M2-W5-a1.json",
    "harness/metrics/attempts/M2-W5-a2-lead.json",
    "harness/metrics/attempts/M2-W5-a3.json",
    "docs/orchestration/m2/reports/M2-W5.md"
  ],
  "acceptance": [
    {
      "command": "pnpm typecheck",
      "exitCode": 0,
      "evidence": "All workspace TypeScript projects completed without diagnostics."
    },
    {
      "command": "pnpm lint",
      "exitCode": 0,
      "evidence": "ESLint completed with zero errors and zero warnings after splitting dialog and record components to meet repository limits."
    },
    {
      "command": "pnpm exec prettier --check .",
      "exitCode": 0,
      "evidence": "All repository files matched Prettier formatting."
    },
    {
      "command": "pnpm exec vitest run --project unit apps/web/test/queries/crm/leads/types.test.ts",
      "exitCode": 0,
      "evidence": "One discovered test file ran with three tests passing, including terminal-stage move semantics."
    },
    {
      "command": "pnpm exec vitest run --project unit apps/web/test/queries/crm/leads/board.test.ts apps/web/test/queries/crm/leads/types.test.ts",
      "exitCode": 0,
      "evidence": "Two discovered test files ran with four tests passing, including deferred lost-drop behavior that confirms the remembered source stage/version without a failure result."
    },
    {
      "command": "pnpm build",
      "exitCode": 0,
      "evidence": "Final Next.js build after the deferred lost-drop fix compiled and emitted /leads, /leads/board, /leads/new, and /leads/[id]."
    },
    {
      "command": "pnpm harness:record M2-W5 --attempt 2 --runner lead --base 6ebbb30 --confirm CONTRACT_DRIFT --took-over",
      "exitCode": 0,
      "evidence": "Attempt 2 recorded format, typecheck, lint, discovered changed tests, and scope all green; CONTRACT_DRIFT was confirmed for the review-driven correction pass."
    },
    {
      "command": "pnpm harness:record M2-W5 --attempt 3 --runner worker --base f4dd4ca --confirm EDGE_CASE,CONTRACT_DRIFT",
      "exitCode": 0,
      "evidence": "Attempt 3 recorded format, typecheck, lint, changed unit tests, and scope all green with truthful worker metadata; EDGE_CASE and CONTRACT_DRIFT were confirmed."
    }
  ],
  "testsAdded": [
    "apps/web/test/queries/crm/leads/types.test.ts"
  ],
  "choices": [
    "Kept table and board as separate routes while sharing one leads query model and the existing KanbanBoard, DataTable, FilterBar, RecordForm, RecordPageLayout, ActivityFeed, and StageSelect composites.",
    "Used optimistic-concurrency expectedUpdatedAt values for stage moves, title edits, conversion, and mark-lost actions, with route revalidation after successful mutations.",
    "Made the convert dialog create a contact and optional organization and deal, and made mark-lost require a configured lost reason while preserving the note.",
    "Moved focused lead helper and terminal-stage tests under apps/web/test so the configured Vitest unit project discovers and executes them.",
    "Default lead lists exclude done_success, done_failure, and cancelled stages; an explicit stage filter includes requested terminal stages.",
    "Board attempts to convert or cancel are rejected with visible feedback, while a lost-stage move opens the lost-reason dialog before markLost.",
    "A deferred lost-stage board move returns a successful confirmation for the remembered source stage/version, preventing a false failure toast and state drift until markLost succeeds."
  ],
  "telemetry": [
    "Attempt 2 was executed by the worker but was recorded with runner=lead and leadTookOver=true; the append-only artifact remains preserved as M2-W5-a2-lead.json.",
    "Attempt 3 corrects the metadata with runner=worker and leadTookOver=false while retaining the confirmed review classes."
  ],
  "openQuestions": [
    "Seeded-data browser verification remains dependent on the local Payload/D1 seed being available when pnpm dev is started."
  ]
}
---

# Report: M2-W5

The Leads vertical is complete in its assigned scope. It includes a searchable and filterable table, a board view with stage moves, new-lead capture, a record page with editable title, stage control, details, activity, conversion, and mark-lost flows. The server layer reads through the current CRM repository and writes through the existing domain commands with expected-version checks.

The worker harness attempt is recorded at `harness/metrics/attempts/M2-W5-a1.json`. Review corrections are preserved in `harness/metrics/attempts/M2-W5-a2-lead.json`, and the truthful worker rerun is recorded at `harness/metrics/attempts/M2-W5-a3.json`; all configured gates are green, including the deferred lost-drop regression and scope check.
