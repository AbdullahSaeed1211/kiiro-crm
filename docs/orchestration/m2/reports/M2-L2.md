---
{
  "wp": "M2-L2",
  "attempt": 1,
  "status": "DONE",
  "filesChanged": [
    "apps/web/package.json",
    "apps/web/src/migrations/20260913_192616_m2_crm.json",
    "apps/web/src/migrations/20260913_192616_m2_crm.ts",
    "apps/web/src/migrations/index.ts",
    "apps/web/src/payload-types.ts",
    "apps/web/src/server/crm/deps.ts",
    "packages/adapters/payload/",
    "packages/modules/crm/",
    "packages/ui/README.md",
    "packages/ui/src/index.ts",
    "pnpm-lock.yaml",
    "docs/decisions/decision-register.md",
    "docs/orchestration/m2/plan.md",
    "docs/orchestration/m2/reports/M2-L2.md"
  ],
  "acceptance": [
    {
      "command": "pnpm exec vitest run packages/modules/crm/test packages/adapters/payload/test/collections/crm-collections.test.ts packages/adapters/payload/test/repositories/crm",
      "exitCode": 0,
      "evidence": "Six files and 155 tests passed, including custom data, organization source, compare-and-set repository writes, and atomic deal closure."
    },
    {
      "command": "PAYLOAD_SECRET=local-db-verification-only pnpm db:reset:local",
      "exitCode": 0,
      "evidence": "The initial and M2 CRM migrations applied to a fresh local D1 database."
    },
    {
      "command": "pnpm typecheck && pnpm lint && pnpm build",
      "exitCode": 0,
      "evidence": "TypeScript and ESLint reported no diagnostics. Next.js built all current routes successfully."
    }
  ],
  "testsAdded": [
    "packages/adapters/payload/test/collections/crm-collections.test.ts",
    "packages/adapters/payload/test/repositories/crm/crm-repository.test.ts",
    "packages/adapters/payload/test/repositories/crm/crm-stage-store.test.ts"
  ],
  "choices": [
    "Normalized absent or malformed Payload customData values to an empty object so the domain contract has one stable shape.",
    "Added organization source to the frozen record contract, collection, codec, generated types, and migration because spec section 10.1 requires it.",
    "Deal stage and closedAt now share one conditional D1 update. The domain's follow-up update becomes a no-op after the repository read confirms the value.",
    "Kept custom field definition storage outside this slice. Conversion copies matching fields when a repository supplies definitions and otherwise copies none."
  ],
  "openQuestions": [
    "The page wave must consume getCrmDeps before the final knip gate; it is intentionally unused until those routes exist.",
    "Field definitions and custom field management remain in a later platform milestone."
  ]
}
---

# Report: M2-L2

The first CRM worker wave is integrated behind the application composition root. The domain commands, Payload collections and repository, and shared UI composites now agree on one record contract. A generated migration applies to a fresh local D1 database, and the current application builds.

The lead review corrected two worker-boundary failures. The repository fixture still modeled the old Payload update path instead of E-017's conditional Drizzle write, and deal closure was split across two writes despite D1 lacking transactions. The fixture now exercises the real access, count, conditional write, and user-scoped read sequence. Deal stage and `closedAt` persist atomically.

The next wave can work as four cohesive verticals: leads, deals, organizations and contacts, and idempotent CRM seed data.
