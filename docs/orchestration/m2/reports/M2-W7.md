---
{
  "wp": "M2-W7",
  "attempt": 7,
  "status": "DONE",
  "filesChanged": [
    "apps/web/src/app/(app)/contacts/",
    "apps/web/src/app/(app)/organizations/",
    "apps/web/src/app/(app)/contact-record-view.tsx",
    "apps/web/src/app/(app)/copy-button.tsx",
    "apps/web/src/app/(app)/directory-filters.tsx",
    "apps/web/src/app/(app)/directory-form.tsx",
    "apps/web/src/app/(app)/directory-list-view.tsx",
    "apps/web/src/app/(app)/directory-record-view.tsx",
    "apps/web/src/app/(app)/directory-view.tsx",
    "apps/web/src/app/(app)/organization-record-view.tsx",
    "apps/web/src/server/crm/directory/",
    "apps/web/test/queries/crm/directory.test.ts",
    "harness/metrics/attempts/M2-W7-a1.json",
    "harness/metrics/attempts/M2-W7-a2.json",
    "harness/metrics/attempts/M2-W7-a3.json",
    "harness/metrics/attempts/M2-W7-a4.json",
    "harness/metrics/attempts/M2-W7-a5.json",
    "harness/metrics/attempts/M2-W7-a6.json",
    "harness/metrics/attempts/M2-W7-a7.json",
    "docs/orchestration/m2/reports/M2-W7.md"
  ],
  "acceptance": [
    {
      "command": "pnpm test",
      "exitCode": 0,
      "evidence": "62 unit test files and 583 tests passed. Existing coverage tooling emitted baseline parser warnings for TypeScript-only UI files but did not fail the run."
    },
    {
      "command": "pnpm --filter web build",
      "exitCode": 0,
      "evidence": "Next.js production build completed, including the organizations and contacts route families."
    },
    {
      "command": "pnpm harness:record M2-W7 --attempt 2 --base m2-crm --runner worker",
      "exitCode": 0,
      "evidence": "Format, typecheck, lint, changed unit tests, and scope gates all passed; harness wrote harness/metrics/attempts/M2-W7-a2.json."
    },
    {
      "command": "pnpm harness:record M2-W7 --attempt 4 --base m2-crm --runner worker",
      "exitCode": 0,
      "evidence": "Final-state format, typecheck, lint, changed unit tests, and scope gates all passed; harness wrote harness/metrics/attempts/M2-W7-a4.json."
    },
    {
      "command": "pnpm exec vitest run --project unit apps/web/test/queries/crm/directory.test.ts && pnpm check:docs && git diff --check",
      "exitCode": 0,
      "evidence": "Directory utility and authorization-boundary tests passed: 1 file, 5 tests; documentation validation and whitespace checks passed."
    },
    {
      "command": "pnpm harness:record M2-W7 --attempt 5 --base m2-crm --runner worker --confirm ACCESS_SCOPE,EDGE_CASE",
      "exitCode": 0,
      "evidence": "Format, typecheck, lint, changed unit tests, and scope gates all passed; harness wrote harness/metrics/attempts/M2-W7-a5.json with confirmed classes ACCESS_SCOPE and EDGE_CASE."
    },
    {
      "command": "pnpm harness:record M2-W7 --attempt 6 --base m2-crm --runner worker --confirm ACCESS_SCOPE,EDGE_CASE,SCOPE_VIOLATION",
      "exitCode": 0,
      "evidence": "Final review-fix state passed format, typecheck, lint, changed unit tests, and scope gates; harness wrote harness/metrics/attempts/M2-W7-a6.json with ACCESS_SCOPE, EDGE_CASE, and the intentionally confirmed SCOPE_VIOLATION classification for existing directory view/test files touched by review fixes."
    },
    {
      "command": "pnpm exec depcruise apps/web/src/server/crm/directory --config tooling/depcruise/.dependency-cruiser.cjs",
      "exitCode": 0,
      "evidence": "The repaired directory subgraph completed with no dependency violations across 14 modules and 28 dependencies."
    },
    {
      "command": "pnpm depcruise",
      "exitCode": 2,
      "evidence": "The requested directory cycles are gone. The repository-wide gate still reports two separate seed-script cycles, scripts/seed/crm-data.ts -> scripts/seed/crm-pipeline-data.ts -> scripts/seed/crm-data.ts and scripts/seed/crm-data.ts -> scripts/seed/crm-directory-data.ts -> scripts/seed/crm-data.ts; those files are outside this repair scope."
    },
    {
      "command": "pnpm harness:record M2-W7 --attempt 7 --base m2-crm --runner worker --confirm CONTRACT_DRIFT",
      "exitCode": 0,
      "evidence": "Attempt 7 passed format:check, typecheck, lint, changed unit tests, and check:scope, and wrote harness/metrics/attempts/M2-W7-a7.json with confirmed class CONTRACT_DRIFT. No additional existing class was accurate for this finding."
    }
  ],
  "testsAdded": [
    "apps/web/test/queries/crm/directory.test.ts"
  ],
  "choices": [
    "Used the existing CRM repository commands for all create and compare-and-set edit writes, keeping authorization and activity behavior in the domain module.",
    "Lists are server-filtered and paginated at 50 rows, with sortable name and updated columns and links from record cells.",
    "Record pages use the shared RecordPageLayout and ActivityFeed, with organization/contact/deal/project relation cards and disabled future tabs clearly labeled.",
    "Owner names are loaded through the request-scoped Payload API, and relation reads use the signed-in user's access rather than overrideAccess.",
    "Contact email and phone have real mail/tel links plus clipboard copy controls; forms normalize blank optional values to null.",
    "Create actions derive ownerId from the authenticated actor, activity reads elevate only after parent authorization, and contact organization options use an unpaginated authorized query.",
    "Form command failures stay inline with user input intact and refresh on conflicts; relation empty states are length-based and external website links accept only HTTP(S) URLs.",
    "Shared directory result types now live in types.ts, so helpers and utils no longer import types from data.ts while data.ts imports those modules. data.ts re-exports the types to preserve its existing public API."
  ],
  "openQuestions": [
    "Delete controls, inline title editing, comments, files, and related task creation remain dependent on later shared actions/composites and are not invented in this scoped vertical."
  ],
  "dependencyRequests": []
}
---

# Report: M2-W7

Implemented and review-hardened the CRM directory vertical for organizations and contacts. Both record types now have searchable, paginated list pages, create flows, guarded edit flows, relation-aware record pages, activity display, owner/details metadata, and contact copy affordances. Review fixes cover server-derived ownership, parent-authorized staff activity reads, complete organization options, resilient form errors, reliable relation empty states, and protocol-safe website links.

The first append-only harness attempt (`M2-W7-a1`) captured a formatter failure that was fixed before the clean attempts. It is retained as evidence rather than overwritten. A post-review full-gate failure exposed two directory import cycles: `data.ts` imported `utils.ts` and `helpers.ts`, while both modules imported shared types from `data.ts`. The root cause was contract drift in the module dependency shape, confirmed as `CONTRACT_DRIFT` in attempt 7.

The countermeasure is the focused `types.ts` extraction. `helpers.ts` and `utils.ts` now depend on the shared type owner, while `data.ts` retains compatibility re-exports. The targeted directory dependency-cruiser check, focused tests, and attempt 7 all pass. The repository-wide dependency-cruiser command still has the two unrelated seed-script cycles listed in the acceptance evidence above. This repair did not weaken dependency rules or change those out-of-scope files.

Remediation commit: `06290ab7f3debc0aee408f5fbf498dfa038d7883`.
