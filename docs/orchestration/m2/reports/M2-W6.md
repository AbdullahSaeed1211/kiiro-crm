---
wp: M2-W6
attempt: 1
status: DONE
filesChanged:
  - apps/web/src/app/(app)/deals/page.tsx
  - apps/web/src/app/(app)/deals/board/page.tsx
  - apps/web/src/app/(app)/deals/[id]/page.tsx
  - apps/web/src/app/(app)/deals/DealBoard.tsx
  - apps/web/src/app/(app)/deals/DealClosingControls.tsx
  - apps/web/src/app/(app)/deals/DealControls.tsx
  - apps/web/src/app/(app)/deals/DealCreateDialog.tsx
  - apps/web/src/server/crm/deals/actions.ts
  - apps/web/src/server/crm/deals/queries.ts
  - apps/web/src/server/crm/deals/view-model.ts
  - apps/web/test/deals.test.ts
  - harness/metrics/attempts/M2-W6-a1.json
acceptance:
  - command: pnpm harness:record M2-W6 --attempt 1
    exitCode: 0
    evidence: "format:check, typecheck, lint, vitest run --project unit --changed, and check:scope all passed; no scope violations."
  - command: pnpm build
    exitCode: 0
    evidence: "Next build completed and listed dynamic routes /deals, /deals/board, and /deals/[id]."
  - command: pnpm exec vitest run --project unit apps/web/test/deals.test.ts
    exitCode: 0
    evidence: "1 test file passed; 2 focused deal view-model tests passed."
testsAdded:
  - apps/web/test/deals.test.ts: stage value aggregation and money formatting.
choices:
  - Reused the existing request-scoped CrmRepository and CRM commands for server authorization, compare-and-set updates, stage invariants, won/lost transitions, and activity creation.
  - Used the existing responsive KanbanBoard; stage headers include the summed stage value while cards show deal value and organization.
  - Reopen moves to the first non-terminal workflow stage through moveDeal; the domain currently has no separate reopen command.
  - Kept currency entry at USD in the create UI because tenant settings/currency query APIs are not part of this WP.
openQuestions:
  - A pnpm dev smoke request reached the deal routes but returned 500 because this worktree lacks the Payload secret environment variable; seeded authenticated runtime acceptance must be rerun by the lead with local env configured.
dependencyRequests: []
---

# M2-W6 — Deals pages

Implemented the cohesive Deals vertical:

- Table view with search, pagination, value, organization, owner, stage, and expected close columns.
- Board view with responsive drag/menu stage changes, terminal collapse behavior from the shared board, and value totals per stage.
- Create deal dialog with optional organization, primary contact, value, and expected close date.
- Deal record page with activity, details, relationships, inline value editing, contact membership updates, stage select, mark won, mark lost with required reason/note, and reopen.
- All mutations are server actions backed by the CRM module and revalidate relevant list, board, and record routes.

The worker worktree is ready for lead review and merge.
