# M2 plan: CRM slice

Re-planned after the M1 spike (GO, `docs/reports/m1-spike.md`) per spec §0.10 and E-020: the owner wants the CRM finished in code first, locally, before more Cloudflare work. This milestone takes the CRM parts of spec M2 (platform), M3 (adapter), M4 (modules) and M5 (UI) as one vertical slice. Platform building blocks are added only as the CRM needs them.

Behavior references (study only, never copy code; licenses in `../references/README.md`): Frappe CRM at `/Users/abdullahsaeed/freelance/mirchmedia-projects/crm/references/frappe-crm` (lead and deal flows, conversion, lost reasons, pipeline views) and Twenty at `.../references/twenty` (record pages, table and board views, activity timeline).

| WP | Owner | Wave | Depends | Status | Objective | Write scope | Acceptance |
|---|---|---|---|---|---|---|---|
| M2-L1 | Lead | 0 | — | merged | CRM contracts: kernel `Money`; `@ops/module-crm` record types and `CrmRepository`/`CrmDeps`; CRM collection slugs, `CRM_FIELDS`, staff scopes and access; CRM sidebar group; local sample tenant | `packages/kernel/src/contracts/`; `packages/kernel/src/index.ts`; `packages/modules/crm/package.json`; `packages/modules/crm/README.md`; `packages/modules/crm/src/ports/`; `packages/modules/crm/src/index.ts`; `packages/adapters/payload/src/contracts/`; `packages/adapters/payload/src/access/`; `apps/web/src/app/(app)/app-frame.tsx`; `tenants/`; `pnpm-lock.yaml`; `docs/` | `pnpm typecheck` |
| M2-W1 | Worker | 1 | M2-L1 | merged | CRM domain and commands (spec §10.1): create and update organization, contact, lead, deal; `moveLead` and `moveDeal` through platform `changeStage` (lost reason required for `done_failure`, deal `closedAt` set and cleared, converted lead stage locked); `markLost`; `convertLead` (idempotent, ordered writes); zod input schemas | `packages/modules/crm/src/domain/`; `packages/modules/crm/src/commands/`; `packages/modules/crm/src/schema/`; `packages/modules/crm/test/` | unit tests against an in-memory `CrmRepository` cover T-CRM-1 to T-CRM-7 |
| M2-W2 | Worker | 1 | M2-L1 | merged | Payload collections contacts, leads, deals, sources, lostReasons, stageTransitions with `CRM_FIELDS`, §11 indexes and `SPIKE_ACCESS` | `packages/adapters/payload/src/collections/`; `packages/adapters/payload/test/collections/` | the collection test covers the new slugs, field types and access |
| M2-W3 | Worker | 1 | M2-L1 | merged | Payload `CrmRepository`: typed get, list, create and compare-and-set update per record type, contact lookup by email, lookups, default workflows, stage store for leads and deals including `stageTransitions` rows | `packages/adapters/payload/src/repositories/crm/`; `packages/adapters/payload/test/repositories/crm/` | unit tests with a fake Payload |
| M2-W4 | Worker | 1 | M2-L1 | merged | UI composites (spec §15.4): StagePill, StageSelect, RecordPageLayout, ActivityFeed, ConfirmDialog, RecordForm (field-config driven create and edit), FilterBar (search and stage) | `packages/ui/src/composites/StagePill/`; `packages/ui/src/composites/StageSelect/`; `packages/ui/src/composites/RecordPageLayout/`; `packages/ui/src/composites/ActivityFeed/`; `packages/ui/src/composites/ConfirmDialog/`; `packages/ui/src/composites/RecordForm/`; `packages/ui/src/composites/FilterBar/`; `packages/ui/test/composites/` | pure logic unit tests; typecheck and lint |
| M2-L2 | Lead | 2 | M2-W1, M2-W2, M2-W3 | merged | Wire CRM collections into the Payload config, migration, `getCrmDeps`, barrels and READMEs; reconcile the worker branches with E-017 and the complete CRM record contract | `apps/web/package.json`; `apps/web/src/migrations/`; `apps/web/src/payload-types.ts`; `apps/web/src/server/crm/`; `packages/adapters/payload/`; `packages/modules/crm/`; `packages/ui/README.md`; `packages/ui/src/index.ts`; `pnpm-lock.yaml`; `docs/`; `harness/metrics/attempts/` | `pnpm build`; migration applies locally |
| M2-W8 | Worker | 2 | M2-W2 | review | Local CRM sample data: agency lead and deal workflows (§18.1), sources, lost reasons, organizations, contacts, leads across stages, deals | `scripts/seed/` | `pnpm db:reset:local && pnpm seed:dev` twice is idempotent |
| M2-W5 | Worker | 3 | M2-L2, M2-W4 | merged | Leads pages (§17.5, §17.6): table and board, record page (details, activity, stage, convert dialog, mark lost), create | `apps/web/src/app/(app)/leads/`; `apps/web/src/server/crm/leads/` | pages work on `pnpm dev` with seeded data |
| M2-W6 | Worker | 3 | M2-L2, M2-W4 | merged | Deals pages: table and board with value per stage, record page (won, lost, value, contacts) | `apps/web/src/app/(app)/deals/`; `apps/web/src/server/crm/deals/` | pages work on `pnpm dev` with seeded data |
| M2-W7 | Worker | 3 | M2-L2, M2-W4 | review | Organizations and contacts: tables, record pages with relations, create and edit | `apps/web/src/app/(app)/organizations/`; `apps/web/src/app/(app)/contacts/`; `apps/web/src/server/crm/directory/` | pages work on `pnpm dev` with seeded data |
| M2-L3 | Lead | 4 | M2-W5, M2-W6, M2-W7, M2-W8 | review | Integrate, `pnpm verify`, report, retro | `docs/reports/`; `harness/lessons/`; `harness/evals/` | CRM slice usable end to end locally |

## Notes
- Local only: no Cloudflare contact in M2 (E-018, E-019). The spike tenants were deleted with owner approval (E-021).
- Workers read the listed reference projects for behavior and edge cases before designing; any code reuse beyond permissive licenses is forbidden (§3).
- M2-L2 found that M2-W3's fake repository still modeled Payload's removed update-by-where path. The lead replaced it with the E-017 access, count, conditional write, read sequence and added an atomic deal stage plus `closedAt` test. Future adapter workers must branch from the current integration head and run repository tests against `writeIfUnchanged`.
- The M2-L3 full gate found dependency cycles in the W7 directory helpers and W8 seed helpers. Both WPs returned to review with append-only remediation attempts; M2-L3 remains in review until the repaired branches pass the full gate.

## Changes from spec table
- Re-planned CRM-first (E-020): spec M2 (kernel and platform), M3 (Payload adapter), M4 (CRM and work modules) and M5 (CRM pages) are cut down to the CRM slice; dashboard, onboarding, settings pages, notifications UI, custom fields UI and templates other than agency move to later milestones.
