# Brief: M2-W8 — Local CRM sample data

- **Milestone:** M2 · **Attempt:** 1 · **Branch:** `wp/M2-W8` from `m2-crm`
- **Objective:** Local CRM sample data: agency lead and deal workflows (§18.1), sources, lost reasons, organizations, contacts, leads across stages, deals
- **Spec references:** §18.1, §21.3 (M2 work packages)
- **Write scope:** `scripts/seed/`
- **Inputs:** merged work packages M2-W2; row in `docs/orchestration/m2/plan.md`
- **Acceptance:** `pnpm db:reset:local && pnpm seed:dev` twice is idempotent; `pnpm harness:record M2-W8 --attempt 1`
- **Boundaries:** do not change spec §0.7 critical paths; no dependency, lockfile or migration changes; follow spec §3, §5.2, §6; decide ordinary implementation details yourself and list them under choices; verify dependency APIs and shared test doubles from this exact worktree before implementation, never from a prior worker report
- **Report:** `docs/orchestration/m2/reports/M2-W8.md` with front matter per `harness/schemas/report.schema.json`

## Known pitfalls
None.

## Previous attempt findings
None (first attempt).
