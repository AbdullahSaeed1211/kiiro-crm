# Brief: M2-W6 — Deals pages

- **Milestone:** M2 · **Attempt:** 1 · **Branch:** `wp/M2-W6` from `m2-crm`
- **Objective:** Deals pages: table and board with value per stage, record page (won, lost, value, contacts)
- **Spec references:** §21.3 (M2 work packages)
- **Write scope:** `apps/web/src/app/(app)/deals/`; `apps/web/src/server/crm/deals/`
- **Inputs:** merged work packages M2-L2, M2-W4; row in `docs/orchestration/m2/plan.md`
- **Acceptance:** pages work on `pnpm dev` with seeded data; `pnpm harness:record M2-W6 --attempt 1`
- **Boundaries:** do not change spec §0.7 critical paths; no dependency, lockfile or migration changes; follow spec §3, §5.2, §6; decide ordinary implementation details yourself and list them under choices; verify dependency APIs and shared test doubles from this exact worktree before implementation, never from a prior worker report
- **Report:** `docs/orchestration/m2/reports/M2-W6.md` with front matter per `harness/schemas/report.schema.json`

## Known pitfalls
None.

## Previous attempt findings
None (first attempt).
