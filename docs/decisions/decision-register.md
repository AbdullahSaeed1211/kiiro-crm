# Decision register

Decisions D-01 to D-50 are defined in `docs/spec.md` §2 and are authoritative there. This register records execution decisions made while building (spec §0.4).

| ID    | Date       | Decision                                                                                                                                                                                                                                                                                                                                                                                                           | Rationale                                                                                                                                                                                                                                                            | Provisional |
| ----- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| E-001 | 2026-09-13 | pnpm 10.34.5 is selected through `packageManager` with `manage-package-manager-versions=true` in `.npmrc` instead of corepack                                                                                                                                                                                                                                                                                      | corepack is not available on the build machine; pnpm 10 switches to the pinned version automatically without a global install                                                                                                                                        | no          |
| E-002 | 2026-09-13 | The Payload `with-cloudflare-d1` scaffold was generated with `create-payload-app@3.89.0 -n web -t with-cloudflare-d1 --use-pnpm --no-deps --no-agent` in a temporary directory and copied to `apps/web` without its `.git`, `.env`, `.yarnrc`, `.vscode` and `.npmrc`                                                                                                                                              | keeps generated secrets and a nested repository out of the product repo                                                                                                                                                                                              | no          |
| E-003 | 2026-09-13 | Until `PLATFORM_DOMAIN` exists, spike tenants `staging-a` and `staging-b` use `workers.dev` hostnames                                                                                                                                                                                                                                                                                                              | M1 needs deployed tenants; the platform domain is a pending human prerequisite (Q-003)                                                                                                                                                                               | yes         |
| E-004 | 2026-09-13 | `peerDependencyRules.ignoreMissing` lists only `monaco-editor` and `yjs`; strict peer dependencies stay on for everything else                                                                                                                                                                                                                                                                                     | Both are optional peers of Payload's admin internals (`@monaco-editor/react` loads the editor at runtime; `@lexical/yjs` is collaborative editing, not used); installing them would add bloat outside the §4.3 allowlist                                             | no          |
| E-005 | 2026-09-13 | Removed the unused `import migrations from './db/migrations'` from the scaffolded `payload.config.ts`                                                                                                                                                                                                                                                                                                              | `create-payload-app@3.89.0` emits a broken import (the folder is `src/migrations`); upstream `templates/with-cloudflare-d1` at payload `06f05f7` has no such import; migrations run via `payload migrate` (§19.3)                                                    | no          |
| E-006 | 2026-09-13 | Remote Cloudflare bindings are opt-in via `PAYLOAD_REMOTE_BINDINGS=1` (lead only); `apps/web/wrangler.jsonc` is a local-only base config (`ops-dev`, no `remote: true`)                                                                                                                                                                                                                                            | The first `next build` evaluated `payload.config.ts` with `NODE_ENV=production`, requested a remote preview session and contacted the logged-in customer Cloudflare account (request rejected, nothing created); builds and local runs must never contact Cloudflare | no          |
| E-007 | 2026-09-13 | M1 spike tenants `staging-a` and `staging-b` deploy to the logged-in first-customer Cloudflare account, as throwaway resources deleted after the spike report; production tenants move to a neutral platform operator account before M8                                                                                                                                                                            | Decided by the owner (Q-002) to unblock the spike; no customer data is used                                                                                                                                                                                          | yes         |
| E-008 | 2026-09-13 | The repository is pushed to a private repository under the owner's personal GitHub account until an operator organization exists                                                                                                                                                                                                                                                                                   | Decided by the owner (Q-004); CI runs there                                                                                                                                                                                                                          | yes         |
| E-009 | 2026-09-13 | shadcn 4.21.0 is set up for the monorepo with hand-written `components.json` files (`packages/ui` aliases `@ops/ui/*`; `apps/web` alias `ui` = `@ops/ui/components/ui`) and `shadcn add <names> -c apps/web`; style `base-nova` is `--base base --preset nova`; the CLI's `cn` package replaces clsx and tailwind-merge; `shadcn` is a dependency of `@ops/ui` because `globals.css` imports `shadcn/tailwind.css` | `shadcn init --monorepo` scaffolds a new Turborepo project and 4.21.0 rejects `-p base-nova`; the files match a reference monorepo generated by the same CLI version                                                                                                 | no          |
| E-010 | 2026-09-13 | Vendored `calendar.tsx` and `sonner.tsx` carry minimal type fixes, listed in `packages/ui/README.md`, instead of relaxing `exactOptionalPropertyTypes` or `noPropertyAccessFromIndexSignature`                                                                                                                                                                                                                     | Spec §3 constraint 5 forbids relaxing gates; the fixes change no runtime behavior                                                                                                                                                                                    | no          |
| E-011 | 2026-09-13 | `apps/web/worker.ts` types its handler as `ExportedHandler<InternalForwardEnv>` and awaits `bridgeInboundEmail` in `email()` instead of `ctx.waitUntil`                                                                                                                                                                                                                                                            | The generated `CloudflareEnv` refers back to `worker.ts` through the self-referencing service binding and the cycle erased the binding types; awaiting lets a failed forward still call `setReject`                                                                  | no          |
| E-012 | 2026-09-13 | The local base Worker in the generated `wrangler.jsonc` declares every tenant binding name (D1, R2, EMAIL, WORKER_SELF_REFERENCE, both rate limiters) with local-only values; types come from `wrangler types cloudflare-env.d.ts --env-interface CloudflareEnv --env-file .dev.vars.example`                                                                                                                      | Local development and generated types cover every binding; the example env file supplies secret names without real values; the output path must precede `--env-file`, which accepts several values                                                                   | no          |
| E-013 | 2026-09-13 | The tenant schema adds `hostType: "workers_dev"` with an optional `host`; `gen-wrangler` then sets `workers_dev: true` without routes and emits `APP_ORIGIN` once `host` is filled                                                                                                                                                                                                                                 | Spike tenants run on workers.dev (E-003) and their hostnames are known only after the first deploy                                                                                                                                                                   | yes         |
| E-014 | 2026-09-13 | Payload config: GraphQL disabled and its routes removed, `idType: 'uuid'`, `defaultDepth: 0`, `maxDepth: 2`, users auth per D-38 with the cookie `secure` flag in production only; the lexical editor and the sample `media` collection are removed; `@payloadcms/storage-r2` returns with the attachments collection in M1-L3                                                                                     | Spec §11 and D-22 (plain text); knip allows no unused dependencies; browsers reject `Secure` cookies on `http://localhost` in some engines                                                                                                                           | no          |
